import { createTheme } from '@mui/material/styles';

// Placeholder theme — the real open-mSupply theme is rebuilt in a later step.
export const theme = createTheme({
  palette: {
    primary: { main: '#e95c30' },
  },
});
