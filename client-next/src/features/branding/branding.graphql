query displaySettings($input: DisplaySettingsHash!) {
  displaySettings(input: $input) {
    __typename
    customLogo {
      value
      hash
    }
    customTheme {
      value
      hash
    }
  }
}
