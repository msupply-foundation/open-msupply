declare global {
    /**
     * @internal
     */
    function __decodeUtf8BufferToString(input: ArrayBufferLike, byteOffset: number, byteLength: number, fatal: boolean, ignoreBOM: boolean): string;
}
export {};
