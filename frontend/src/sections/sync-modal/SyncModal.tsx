import type { Component } from 'solid-js';

// PLACEHOLDER — generated nightly from spec/sync-modal; see syncIndicator.ts.
// Inert until the generation run replaces it (the chrome Sync entry opens
// nothing visible).
export const SyncModal: Component<{
  open: boolean;
  onClose: () => void;
}> = () => null;
