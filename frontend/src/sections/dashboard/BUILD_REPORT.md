# Build report — dashboard

**Target stack:** SolidJS + Vite; shared component library `src/ui/` resolved through [`spec/ui-standards/components.md`](../../../spec/ui-standards/components.md).

Regenerated from `spec/dashboard/` (scoped `/spec-build dashboard`). One screen (S1) mounted at both the store root `/` (the landing screen) and the `dashboard` nav destination. Wire surface: the six split count queries in `dashboardCounts.graphql`; the display-gate preferences ride on the shared guard-3 `storeContext` query (no new wire fields — the `PreferencesNode` fields were already present). Gates green: `pnpm check`, `pnpm test` (596 total; 46 dashboard across 5 files), `pnpm build` (DashboardPage = its own lazy chunk, 14.43 kB / **3.85 kB gzip**).

**The dashboard owns the plugin-region semantics** (branch `448-dashboard-plugin-semantics`): how contributions merge with built-ins and how suppression by published id behaves — the published-id tree (`DASHBOARD_IDS`) plus the pure, unit-tested `mergeRegion` in `regions.ts` (deterministic anchor/order/id placement, suppression, missing-anchor diagnostics). OMS-REG-DB-02 (+ OMS-REG-DB-01.58) are covered at the logic level.

The **render integration** of those semantics (mounting contributions, honouring suppression at render) is deliberately **deferred to the plugins vertical** (greenfield). The page stays explicit composition — built-ins render directly, gated only by their preference gates — rather than pre-placing plugin mount points against an empty contribution set. Rationale: matching open-mSupply's real shape (`client/packages/dashboard/src/hooks.tsx` merges core + plugin into one list _only because plugins exist there_), the merge-and-render wiring belongs with the consumer; pre-placing it now would either scatter render scaffolding at every container (overkill for zero contributions) or drive rendering through a `<For>` over freshly-merged entries, which risks Solid remounting the stats on every count update (kdd/solid-reactivity-pitfalls). So the semantics ship and are tested; the one deliberate place to add the render integration is the plugins vertical.

## AC coverage

| Behaviour                          | Test                                                | Notes                                                                                                                                                                                                                                                                |
| ---------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OMS-REG-DB-01.21/.24               | `dashboardCounts.test.ts`                           | wire surface is queries only, all `storeId`-scoped; store-switch refetch is resource keying (code-level)                                                                                                                                                             |
| OMS-REG-DB-01.22/.23               | `panelState.test.ts`                                | forbidden → in-panel permission error, unexpected → in-panel generic error; per-panel isolation is one resource per family (`DashboardPage.tsx`)                                                                                                                     |
| OMS-REG-DB-01.58 / OMS-REG-DB-02.9 | `regions.test.ts`                                   | exactly the three built-in widgets + their documented panels/stats (31 published ids, all unique); built-ins composed with preference gates in `DashboardPage.tsx`. A-plugin-MAY-add/suppress is the `mergeRegion` semantics; render integration deferred (see flag) |
| OMS-REG-DB-02.2–.5                 | `regions.test.ts`                                   | `mergeRegion` deterministic order: before/after/end anchor, then order, then id; input-order independent; unresolved/hidden/suppressed anchor → end + diagnostic (logic level; render integration deferred)                                                          |
| OMS-REG-DB-02.6–.8                 | `regions.test.ts`                                   | `mergeRegion` suppression removes only built-ins, never a contribution (logic level; render integration — incl. the widget/panel→nested cascade — deferred to the plugins vertical)                                                                                  |
| OMS-REG-DB-02.10/.11               | `regions.test.ts` (partial)                         | gated-off external panel never fetches (resource source pauses — a hidden piece costs nothing); contribution render-isolation is the `PluginRegionOutlet` error boundary, exercised when the plugins vertical mounts it                                              |
| OMS-REG-DB-01.33/.34/.35           | `dashboardCounts.test.ts`, `statLinks.test.ts`      | count values server-computed (C2 below); window/status link filters tested                                                                                                                                                                                           |
| OMS-REG-DB-01.36                   | `dashboardGates.test.ts`                            | procurement gate both states; partition is server-side                                                                                                                                                                                                               |
| OMS-REG-DB-01.40                   | —                                                   | server-computed count wired verbatim (`request.draft`); C2 gap                                                                                                                                                                                                       |
| OMS-REG-DB-01.37                   | `statLinks.test.ts`                                 | link restates New/Allocated/Picked; count server-computed                                                                                                                                                                                                            |
| OMS-REG-DB-01.38                   | —                                                   | server-computed count wired verbatim (`response.new`); C2 gap                                                                                                                                                                                                        |
| OMS-REG-DB-01.39                   | `dashboardGates.test.ts`                            | program-module gate both states; alert emphasis at > 0 in `DashboardPage.tsx`                                                                                                                                                                                        |
| OMS-REG-DB-01.41                   | `statLinks.test.ts`                                 | expired link ≤ today; count server-computed                                                                                                                                                                                                                          |
| OMS-REG-DB-01.42/.46               | `statLinks.test.ts`, `dashboardCounts.test.ts`      | D = 30 sent explicitly; subtraction is server-side (validated live per spec)                                                                                                                                                                                         |
| OMS-REG-DB-01.44                   | `statLinks.test.ts`                                 | 30–89-day link window, 90th day excluded; count server-side                                                                                                                                                                                                          |
| OMS-REG-DB-01.45                   | `dashboardGates.test.ts`, `statLinks.test.ts`       | gate both states + threshold-day link window                                                                                                                                                                                                                         |
| OMS-REG-DB-01.53, OMS-REG-DB-01.47 | —                                                   | server-computed counts wired verbatim (`total`, `noStock`); C2 gap                                                                                                                                                                                                   |
| OMS-REG-DB-01.48                   | `dashboardGates.test.ts`                            | look-back gate both states                                                                                                                                                                                                                                           |
| OMS-REG-DB-01.49, OMS-REG-DB-01.50 | —                                                   | server-computed (`lowStock`/`highStock`); thresholds sent explicitly (OMS-REG-DB-01.54)                                                                                                                                                                              |
| OMS-REG-DB-01.51                   | `dashboardGates.test.ts`                            | alert-threshold gate both states                                                                                                                                                                                                                                     |
| OMS-REG-DB-01.52                   | `dashboardGates.test.ts`                            | gate both states; the threshold-0 degenerate count is never rendered                                                                                                                                                                                                 |
| OMS-REG-DB-01.54/.46               | `dashboardGates.test.ts`, `dashboardCounts.test.ts` | explicit low/high from store prefs; fetch pauses until prefs resolve; `daysTillExpired` always 30                                                                                                                                                                    |
| OMS-REG-DB-01.55/.57               | `statLinks.test.ts`                                 | inbound/outbound/stock/**item-catalogue** link filters restate their counts (decoded through each list's own filter shape); still-unbuilt lists (internal-order, customer-requisition) land on registered placeholders unfiltered (the AC's carve-out)               |
| OMS-REG-DB-01.56                   | —                                                   | permission check (`hasPermission` → `reportPermissionDenied`) + handoff in `DashboardPage.tsx`; no component-test env — verify against the app                                                                                                                       |

## Flags

- **C2 (real backend):** no CI tooling exists for the real-backend leg. Every count value is server-computed and wired verbatim; the client-side logic (gates, links, thresholds, panel states, region semantics) is covered at the logic level. The spec's validation notes record live verification of the counts.
- **Plugin-region render integration is deferred to the plugins vertical (OMS-REG-DB-02 (+ OMS-REG-DB-01.58)).** The dashboard owns and tests the semantics (`regions.ts`: published-id tree + `mergeRegion` — deterministic before/after/end placement, `order`/`id` tiebreak, load-order independence, suppression-of-built-ins-only, missing/hidden/suppressed-anchor → end + diagnostic). It does **not** pre-wire the render against an empty contribution set. Two reasons: (1) matching open-mSupply's real shape — `client/packages/dashboard/src/hooks.tsx` merges core + plugin into one list _only because plugins exist there_, so the merge-and-render belongs with the consumer; (2) Solid-specific — a `<For>` over freshly-merged entries would risk remounting the stats on every count update unless carefully memoised (kdd/solid-reactivity-pitfalls), whereas the current explicit-composition page is remount-safe by construction. When the plugins vertical lands it consumes `mergeRegion` and mounts contributions (each in a `PluginRegionOutlet` error boundary — OMS-REG-DB-02.11) in that one deliberate place; the suppression cascade (widget/panel → nested pieces) then falls out of the page's nesting.
- **Stock-level links (item catalogue):** the six filtered stock-level stats (out-of-stock all / recently-used, at-risk, low-stock, high-stock, overstocked) link into `catalogue/items` filtered to their counts; total items + the panel title stay unfiltered. The dashboard conforms to the items list's **own** URL filter contract (`ItemsListFilter`: the `lens` stock-status enum + `atRisk` + `min/maxMonthsOfStock`) — the list owns its encoding and expands the lens to the wire `ItemFilterInput` itself; `spec/dashboard/contract.md § navigation correspondence` records this vocabulary. Only the customer-requisition stats remain on an unfiltered placeholder (requisitions vertical unbuilt); the internal-order draft stat now filters `status=DRAFT` via the internal-orders list's own contract.
- **Internal vs external inbound links (contract › navigation correspondence):** ~~not expressible~~ — the inbound list has since shipped its origin `kind` URL filter, and the **external** panel's links (title + stats) now carry `kind=fromPurchaseOrder` (exactly the PO-linked count set). Still open for the **internal** panel: "internal" = `manual` ∪ `fromInternalOrder`, and the single-select `kind` has no value covering both, so internal links remain window/status-only (recorded fallback).
- **Order more (OMS-REG-DB-01.56):** resolved — the shortcut now opens the internal-orders vertical's own create flow (permission gate → recent-stocktake warning gate, spec/internal-orders AC-C1/C5 → `CreateInternalOrderModal`), navigating to the created order's detail. The warn preference rides the guard-3 store context; the insufficiency check and both dialogs lazy-load on first use.
- **Cross-section create-flow imports:** the New inbound / New outbound shortcuts lazily import `CreateInboundShipmentModal` and `CustomerSearchModal` from their owning sections (self-contained modals; loaded on first use). If more consumers appear, hoist to `src/domain/`.
- **Codegen drift in other verticals (C7):** running `pnpm codegen` regenerated `customer-returns` and `outbound-shipments` generated files against the newer pinned schema; those regenerations were reverted (out of scope). Those verticals need a regen + filter-map pass on their next build.
- **Expiring-soon link boundary:** resolved — the soon link now spans tomorrow…today+30, exactly the count's window (spec divergence **D74**; the current app's today…+1-calendar-month link is the recorded delta).
- **Locale keys:** all count/label strings resolved to existing catalog keys verbatim; **no new keys minted**. The plugin-region error fallback (ui-surface S2 flags it as genuinely new UI with no key to cite) is minted by the plugins vertical when it builds the render integration — not pre-minted here, since nothing renders it yet.
- **Test hooks (C8):** `e2e/TESTIDS.md` defines no dashboard-specific ids; nothing to place.

## Candidate spec refinements

- The plugin-region **render integration** (mounting contributions via `mergeRegion` + `PluginRegionOutlet`, honouring suppression, minting the error-fallback key) lands with the plugins vertical — the dashboard already owns and tests the semantics. Decide there whether contributions interleave between built-ins (full `mergeRegion` order) or the `PluginRegionOutlet`'s current last-child design suffices, and how to drive the render without Solid remounts (memoised merge).
- The internal panel's links can't yet be narrowed to internal-only — needs the inbound list's `kind` contract to grow a selection covering `manual` + `fromInternalOrder` (external is done — see flag above).
- State what a create shortcut should do while its owning vertical is unbuilt (this build applied OMS-REG-DB-01.57's placeholder rule by analogy; since resolved for Order more — the customer-requisition links are the remaining placeholder use).
