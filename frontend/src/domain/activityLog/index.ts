// The shared activity-log (record "Log" tab) module (kdd/domain-modules): one
// record-scoped query + the read-only panel that renders its Date/Time/User/
// Event/Details table. Consumed by any detail view that shows a record's log.
export { ActivityLogPanel } from './ActivityLogPanel';
