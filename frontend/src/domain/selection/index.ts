// The Selection domain module (kdd/domain-modules + kdd/action-modal): a reusable bulk-action modal
// for acting on a selection, with a confirm → working → success | error phase machine.
export {
  SelectionActionModal,
  type SelectionActionModalProps,
  type SelectionActionResult,
} from './SelectionActionModal';
