# UI elements — what each is built from

A living ledger of every UI element built in this app and **what went into it** — hand-rolled plain HTML/CSS vs. a headless library part, and _why_ a library was used where it was. Update this whenever an element is added or its composition changes. (Same convention as the RnD prototype's `UI_ELEMENTS.md`.)

> **Convention.** Default is **hand-rolled**: plain semantic HTML + CSS Modules + design tokens (`src/ui/styles/tokens.css`), sized in rem. We reach for a **headless** primitive only where the widget has a real accessibility/interaction contract that's tedious and risky to hand-roll (and that WCAG 2.2 grades). When we do, **we still own all the markup and CSS** — the library supplies behaviour + ARIA, not a look.
>
> **The WCAG 2.2 AA bar applies to every element in this table, hand-rolled or headless** (principle #9 in `CLAUDE.md`). Buying a headless primitive is how we meet it for the hard widgets; it is not the only place a11y matters. Hand-rolled elements owe the same duties — semantics + accessible name, keyboard operability, visible focus, AA contrast in both themes, no colour-only meaning.

## Elements

| Element / part                                                                                                                             | Built with                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Headless dep — and why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Storybook shell** — header, section menu, panel                                                                                          | The library's **real MenuBar** for the section menu (dogfooded — see [`kdd/showcase-harness`](../../../kdd/showcase-harness/draft-kdd.md)) + a hand-rolled main column (`ui-showcase/ShowcaseApp.tsx`: header strip with title/ThemeToggle/overlay-hamburger, scrolling panel)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — Sections are grouped **Components / Layout Elements** (`category` on each `SectionDef`; the MenuBar nav model is derived from the registry, group icons on the `categories` list). Active section lives in the URL hash (`#/showcase/buttons`) — the MenuBar's `onSelect` just writes the hash, one `hashchange` listener owns the state; the app's router is not in play here. The shell keeps its own hamburger in overlay mode (the library Header's shell-context one is a page-region affair). Lives in `src/ui-showcase/` with all other demo scaffolding — the library (`src/ui/`) and the app never import from there; the ONLY entry is the **dev-only `#/showcase` branch in `src/index.tsx`**, dead-code-eliminated from production builds so no showcase chunk is emitted at all (see `ui-showcase/README.md` + [`kdd/showcase-harness`](../../../kdd/showcase-harness/draft-kdd.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Application shell** (`ui/layout/AppShell`) — the app-level container                                                                     | Hand-rolled (flex row: MenuBar + main column of the **content slot** (the current page) and the **orange app footer**)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | — Pure layout + tokens, and **the container the main menu lives in — not the pages** (see [`kdd/page-composition`](../../../kdd/page-composition/draft-kdd.md)): one AppShell mounts per host and pages swap inside its content slot, so menu state (rail collapse, overlay) survives navigation. Per-page geometry (pinned header, scrolling body, side panel, content footer) belongs to the **Page frame** the page composes — the shell's former `header`/`contentFooter` slot props moved there. The one responsive decision (docked rail vs. hamburger overlay) is a "which element renders" branch driven by `useIsNavOverlay` (JS `matchMedia`), per principle #7; the shell provides `ShellNavContext` (isOverlay + openNav) so the hamburger renders inside the page's Header strip. Footer is the current app's orange status bar (General / Edit / demo / language / Central server) — `--primary-main` ground, white content via `color-mix` for the hover/divider tints. **Controlled**: `selected`/`onNavigate` — the host owns nav selection (`src/nav/ShellLayout` hosts it, driven by the real `@solidjs/router` since the integration merge; the nav model derives from `src/nav/navConfig`). The footer LanguageSelector now drives the real i18n locale (`src/intl`) and `App.tsx` owns document direction — a known library→app import to consolidate (see [`kdd/showcase-harness`](../../../kdd/showcase-harness/draft-kdd.md)). Adapted from the RnD prototype's App shell.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Page frame** (`ui/layout/Page`) — the page-pattern geometry                                                                              | Hand-rolled (flex column: `header` slot / middle row of scrolling body + `sidePanel` slot / `contentFooter` slot)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | — **Geometry only** — the structural contract every list/detail-style page shares, with zero opinion about slot contents: the frame owns where regions sit and what scrolls (body only; header/panel/footer pinned), regions own their own look, optional slots collapse (list and detail are one frame, not two templates). Pages compose it inside an AppShell and own **no CSS of their own** (enforced by `scripts/check-page-css.mjs` in `npm run check`). The canonical assemblies live as prose recipes in `PAGES.md` (the `pages/OutboundShipments` skeletons were retired at the integration merge — the stocktakes vertical is the live reference). See [`kdd/page-composition`](../../../kdd/page-composition/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Side panel** (`ui/layout/SidePanel`) — SidePanel + SidePanelSection + SidePanelSubheading + SidePanelActions                             | Hand-rolled (`<aside>` docked in the Page frame's `sidePanel` slot, own scroll; divider-separated `<section>`s; field rows are plain `<dl>` dt/dd pairs styled by the panel's CSS) — except the `collapsible` mode, which composes the shared `Accordion`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | — The detail-view right-hand panel (the current app's DetailPanel: additional info, related documents, comments). Pure layout, near-zero state — the page composes sections and owns the data (open/close boolean via the co-located `createSidePanelOpen` helper: responsive default — open at/above the `sidePanelDefaultOpen` breakpoint — with the user's explicit choice persisted in localStorage; spec ui-standards/layout.md → page regions). **`SidePanelSection` has an optional `collapsible` mode**, composing the shared `Accordion` (`ui/elements/accordion/Accordion`) as its own standalone single-item instance — one `Accordion`+`AccordionItem` per collapsible section, never a root shared across sibling sections, matching the real app's independent-disclosure usage (`ReportsPage.tsx`'s two side-panel sections toggle independently). **Every section names itself**: a required `value` — the section's semantic key, a fixed kebab-case identifier, never `t()` output (the `TabDef.value` identity/label split) — stamped as its `panel-section-<value>` container testid (the suites' scoping hook for content assertions, the `cell-<columnId>` pattern; emitted uniformly as component behaviour, like all shared ids). `collapsible` is purely behavioural: it forwards `value` as the `AccordionItem`'s key (hence the `accordion-trigger-<value>` testid) and `defaultOpen` as the item's `defaultValue`. **One heading convention per panel:** the section passes `sectionTrigger`/`sectionContent` classes (scoped `.section .x` so they deterministically beat the Accordion module's own single-class spacing) so a collapsible heading renders in exactly the plain `.title` box — flush inline, same block rhythm. The heading stays an `<h2>` for the outline; the non-collapsible default renders `<h2>` + children directly. (Formerly a hand-rolled disclosure duplicating the same convention `Accordion` now owns canonically — consolidated 2026-07-22.) **`SidePanelSubheading` groups fields within a section**: a bold `<h3>` under the section's `<h2>` (e.g. the Charges panel's "Stock charges" / "Service charges" pricing groups), with a gap below separating it from its group's rows and an optional `action` slot pinned inline-end (a group's Edit button). **The record-actions section pins at the panel's end**: `SidePanelActions` (the plain one-action-per-row cluster) is composed inside the panel's last section (`value="actions"`, titled `heading.actions` — the reference assembly is `StocktakeSidePanel`), and the panel's CSS pins the section containing the cluster (`.section:has(> .actions)` — the Header ↔ TabList `:has` convention; sticky bottom + flex auto-margin in the scrolling panel column, matching the current app's non-scrolling footer row). Explicit composition per the page-composition KDD — no rigid pre-assembled actions component. **Composition/authoring spec: [SIDE_PANEL.md](./SIDE_PANEL.md)** — what the panel enforces for free vs the composition choices a vertical still makes (group headings, input sizing, calculated-figure helper text, inline actions), with copy-paste field recipes. Demoed at `#/showcase/side-panel` (Layout Elements): a toggle button slides it out over the page from the inline-end edge, full viewport height — the slide rigging (fixed holder + transform, the MenuBar overlay pattern mirrored; reduced-motion honoured) is showcase-owned, not the component's. **Narrow screens:** below the compact breakpoint the OPEN panel becomes a full-screen overlay over the page region (Page.module.css media query; Page.tsx puts `inert` on the covered main column and closes on Esc — spec D27); the docked push-panel is unchanged at compact and above, and the closed-state clip (no-remount contract) is untouched. Interim treatment — the dialog-semantics Drawer replacement still arrives with the Feedback work.                                                                                                                                                                                                                                                                                                                                                           |
| **MenuBar** (`ui/layout/AppShell/MenuBar`) — the main menu, two modes                                                                      | Hand-rolled (`<nav>`/`<ul>`, `<button>` nav items with `data-selected`/`data-active`, `aria-expanded` collapsible sections; overlay panel + scrim)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — One component, two layouts: **docked** (icon-rail collapsible via an explicit chevron toggle beside the brand mark — the mark itself is not a button; spec/chrome § sidebar wants the toggle explicit, and a logo that collapses the nav is not discoverable) and **overlay** (off-canvas panel + scrim below the `navOverlay` breakpoint), sharing the same nav lists. **The nav model is host-supplied props** (`upper`, optional `lower` — 2026-07-08): AppShell passes the app's `navModel`, the showcase shell passes its section registry; the component stays purely presentational. Collapsible sections are a plain signal + `<Show>` (no Collapsible primitive — it's just show/hide, no focus/ARIA contract worth buying), and the open one is **single-accordion state owned by MenuBar**, not by each section: "open this" means "close that", which no individual section can decide; the section holding the selected destination opens itself via an effect on `selectedId`. **Collapsed, children are reached through `NavFlyout`** (co-located) — labels are hidden, so without it collapsing the rail would put every child destination out of reach. It is one shared native `popover="manual"` panel (top layer, so the rail's `overflow: hidden` can't clip it; `manual` because the rail's hover/focus intent drives it and light dismiss would fight the hover grace period), shown by an effect on the open target and positioned beside the anchor with measured geometry, mirroring the anchor side in RTL. Hover opens after a 110 ms intent delay, focus opens at once, `→`/`Enter` steps focus in, `↑`/`↓` walk the rows and `Esc`/`←` return focus to the icon (guarded so that focus-return can't immediately re-open it). A section whose child is the active destination carries an accent bar on the rail — its highlighted child is inside the flyout, so the rail needs its own non-colour cue. Item layout matches the current app: `[icon][chevron·or·slot][label][end-chevron]`, `--nav-item-height` (2.5rem) section rows and denser **`--nav-child-height` (1.875rem) child rows** — the app's drawer spec; the child token was ported from theme.ts but only wired up 2026-07-09, when the integration merge's real nav data made the too-tall children visible, label `--text-body`, orange active parent, orange end-chevron on the selected item. RTL via logical properties. Nav items are inert buttons in the showcase demo; in the app the host wires them to the router. Renamed from `Sidebar` (Carl, 2026-07-08) — **the "Sidebar" name is reserved for a different, future element**; don't reuse it for the menu. Entries are **destinations only** — sync is not one of them: the site's standing sync signal is the bottom bar's **SyncStatus** cell, so nothing here carries a badge or a state slot (spec/chrome § sync status).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **LanguageSelector** (`ui/layout/AppShell/LanguageSelector`) — language picker (mounted by Settings › Display, not the footer)             | Our markup + CSS over Kobalte DropdownMenu (Trigger/Portal/Content/Item)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Kobalte DropdownMenu** — a menu popup with focus management, type-ahead and arrow/Escape keyboard nav worth buying; styled via `data-*` (`data-highlighted`, our `data-current`), opens upward by default (`placement` prop, default `top-start`) out of the footer — a header host passes `bottom-end` (the showcase header hosts one so locale effects — RTL flip, number/date formatting — are visible on every section). Native language names, RTL-tagged, ported from the prototype's Radix version. Picking an RTL-tagged locale (ar/prs/ps) flips the whole document to RTL — the host (AppShell, and the Login page the same way) sets `dir` on `<html>` and restores `ltr` on unmount. Hover/focus tints derive from `currentcolor`, so the trigger works on any surface (white-on-dark in the footer, dark-on-light on the login panel).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **SyncStatus** (`ui/layout/AppShell/SyncStatus`) — bottom-bar sync cell                                                                    | Hand-rolled (two `<button>`s in one tone-scoped group; the in-flight spin is CSS `@keyframes` over the glyph)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | — The site's standing sync signal, pinned to the bar's inline end. Presentational: the host passes an already-resolved `label` + `detail` + `tone` + `dimmed` + `syncing`, because what a wire status MEANS is the sync-modal vertical's derivation. **Two controls, deliberately split** — the status line starts a sync on one click (no dialog, `aria-disabled` mid-run so it keeps focus; in-flight is armed at the CLICK via the shared SYNC-03.25 trigger machine, since a fast-failing run never produces an `isSyncing` frame to observe), a small details button opens the sync modal; making the line open the modal would put a dialog in front of the thing users came for, and making the whole cell sync would strand the run detail. **Tone selects the GLYPH, never a colour** — warning swaps in the alert triangle, error the crossed circle (not the exclamation circle: the two are barely separable at 16px, and the crossed circle also says "did not happen"). Colour never touches the bar's ground, which is store data (brand orange on a central server, an arbitrary hex where a store sets one): it cannot be held to a ratio — `--error-main` measures 1.23:1 on the orange — and worse, a hue landing near the store's own vanishes, so on a green bar a green mark is a mark nobody can see. Text and glyph keep the bar's own content colour at every state, which makes colour independence structural rather than label-dependent. One level sits BELOW the tones: `dimmed`, for an unreachable server — dimming is not a hue, so it survives an unknown ground, and the dimmed cell takes the shared `WifiOffIcon` — the same mark `ErrorDialog` raises for an unreachable server, so an outage looks the same wherever it surfaces — a mark naming its cause, which outranks any tone glyph (what a user can act on is that the link is down, not that a threshold was crossed). The in-flight animation ROTATES that glyph on `--motion-spin`, the shared indeterminate-progress token, which slows rather than stops under `prefers-reduced-motion` — a frozen indicator reads as a hung app.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **UserMenu** (`ui/layout/AppShell/UserMenu`) — footer signed-in-user cell + popup                                                          | Our markup + CSS over Kobalte DropdownMenu (Trigger/Portal/Content/Group/GroupLabel/Item)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Kobalte DropdownMenu** — same buy as LanguageSelector (focus management, Escape, focus return to the trigger — OMS-REG-FTR-01.17 / D6). Identity-card layout: an initials avatar (`--bg-group-main` disc, decorative/`aria-hidden` — the name it abbreviates sits beside it) + display name with the job title as its subtitle, then username + email in a recessed `--bg-group-light` block, then logout as the sole menu item behind a full-bleed hairline (the SplitButton dropdown idiom), its `PowerIcon` in `--primary-main` as in the current app. The identity is static text, so it sits in a `Group` labelled by the display name rather than loose inside the `menu` role; each contact row leads with an icon and carries its field name as a **visually hidden** label, so the row still reads "Email: …" and still translates without spending a line. The popup sizes to content (`max-content`, capped at `min(26rem, 100vw - 2rem)`) so a normal-length address stays on ONE line; an outlier ellipsises with the full string in its `title`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Page header** (`ui/layout/Header`) — Header + HeaderButtons + Toolbar                                                                    | Hand-rolled (one flex-wrap `<header>` strip; the parts slot themselves via their own CSS — HeaderButtons `margin-inline-start: auto`, Toolbar `flex-basis: 100%`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | — The page-top atom (the current app's AppBar): breadcrumb inline-start, page actions inline-end, per-page toolbar on its own full-width row. **Pure layout, zero state** — the page composes `<Header><Breadcrumb/><HeaderButtons/><Toolbar/></Header>` and owns the trail data and action handlers. Squeezed, the buttons wrap intrinsically below the breadcrumb (no breakpoints, principle #7). Toolbar is deliberately **not** `role="toolbar"` (that role demands roving-focus arrow-key semantics — wrong for a loose filter strip). Inside an AppShell the **overlay hamburger renders at the strip's start automatically** (via `ShellNavContext`; standalone headers, e.g. the showcase panel, never show one), and the page hands the composed header to the Page frame's `header` slot. Ported from the RnD prototype's Header, tab strip included: a `ui/elements/tabs/Tabs` **TabList as the last child becomes the header's bottom edge** — `Header.module.css` matches `:has(> [data-tab-bar])`, drops its own border/bottom padding and pulls the strip out to its edges, so the strip's border runs edge to edge as in the demo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **HeaderToolbar** (`ui/layout/Header/HeaderToolbar`) — the header field cluster                                                            | Composes the generic `Toolbar` — wraps `children` in a `FormRow` inside a grow container, plus an optional `alert` slot                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | — The enforced layout for a page-header **field cluster** (a document's editable + read-only header fields), so pages don't hand-roll it. `children` (inputs + read-only `LabelledValue`s) flow into a `FormRow` (equal shares at `minFieldWidth`, default 10rem, growing to fill and packing as-many-per-row-as-fit); a grow container makes the row fill the toolbar. The optional **`alert`** prop takes a compact `<Alert>` (the persistent-context meta chip), rendered at the row's end as a content-hugging chip (Alert's own `flex: 0 1 auto`) pinned to the **bottom baseline** (`.fields [data-compact]`) — so it rides the row when there's room and drops to its own line when not, while the field labels line up along the top. Because the row top-aligns (which is what keeps an error inside its own field), a **slot floored below its label's width** wraps that label and drops its control half a row: cap a narrow field with `maxWidth`, not a lowered `minWidth`It must be `compact`: a full-width Alert is a plain FormRow item and takes an equal share of the row like a field. A non-Alert trailing chip (e.g. a `ToggleSwitch`) opts into the same bottom-hug with an inline `flex: 0 1 auto; align-self: flex-end`. A field's own `error` stays inside the field (never a sibling Alert), so the message extends that field downward and leaves the rest of the row where it was. **A CSS-grid rewrite of this component was tried and reverted (Carl 2026-07-29)** — the grid model was a mistake; the FormRow shape above is the standard. Weighting a field’s share is therefore done **per item inside that unchanged FormRow model** — a `FormRowItem` wrapping one child, flex weights only, equal shares still the default — never by reinstating a row-level column template. The generic `Toolbar` stays pure layout for its other uses (a list `FilterBar`, a stub). Demoed at `#/showcase/header` (four field mixes — a field cluster, the same cluster with weighted shares, one carrying a field `error`, and a settings row) and used by the Detail-table page example. Real pages on it: prescriptions (`src/sections/prescriptions/detail/PrescriptionToolbar.tsx`), **inbound shipments** (editable pickers + a locked date + read-only PO values + the kind banner as the `alert`), **patients** (an all-read-only cluster of `LabelledValue`s). Their fields carry their own labels at `size="small"`, and promoted custom fields join them via `CustomFieldsToolbar`'s `layout="field"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Breadcrumb** (`ui/layout/Header/Breadcrumb`)                                                                                             | Hand-rolled (WAI-ARIA breadcrumb pattern: `<nav aria-label="Breadcrumb">` > `<ol>`; ancestor crumbs with `to` are bold `<a>` links, the leaf is the page's **`<h1>`** with `aria-current="page"`, styled as just another crumb; `/` separators are `aria-hidden` list items; brand-orange section icon, taken from the shell)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | — Links + separators carry no interaction contract worth buying. The trail is a plain `crumbs` prop — page-owned now, route-derived once routing lands; a crumb's interim `onClick` (added for the detail → list back-navigation while views swap via local state) renders as a link and gets replaced by a real routed href later. **The leaf is the page's single h1** (decided at shell integration, 2026-07-08 — the page title lives in the breadcrumb tail, as in the current app; pages must not render another h1). Ported from the prototype's Breadcrumbs (leaf was a plain span there). **The leading icon is the route's nav-group glyph**, supplied by `ShellLayout` through `shellContext`'s `ShellSection` bridge (same shape as the Header's hamburger bridge) rather than hard-coded per page — spec/ui-standards › layout, page regions; an explicit `icon` prop can still override it, though no screen does. A **record-kind** glyph is per-crumb instead (`Crumb.icon`, rendered `aria-hidden` before that crumb's label so the leaf `<h1>`'s accessible name stays the record's) — the inbound shipment detail's truck/house before the shipment number.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Tab bar** (`ui/elements/tabs/Tabs`) — Tabs + TabList + TabPanel                                                                          | Our markup + CSS; the sliding underline is our own measured-geometry indicator (the one legit px use — computed layout, not a design token; re-measures on tab change, window resize and webfont load). `TabList` reads the active tab from Kobalte's context — no `value` prop                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Kobalte Tabs** — the WAI-ARIA tabs pattern + roving tabindex + ←/→/Home/End + automatic activation + tab↔panel wiring; inactive panels unmounted; the panel is a tab stop only when nothing inside it is (upgrade over Radix). Tiny; no Floating UI. Solid analogue of the prototype's Radix Tabs. Kobalte's own `Tabs.Indicator` rejected (no re-measure on container resize → stale underline on a centred strip), and its missing `aria-labelledby` on the initially selected panel is patched by passing it explicitly — see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md). The indicator re-measures via a **ResizeObserver on the strip** (a window resize listener fired before the menu bar's docked↔overlay swap reflowed the strip, stranding the underline), plus tab change and webfont load. The root is **`display: contents`** (a state boundary, not a layout box) so a `<Tabs>` can wrap a `<Page>` frame from outside — TabList in the header slot, panels in the body. Rendered as a Header's last child it becomes the header's bottom edge (see the Page header row).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Accordion** (`ui/elements/accordion/Accordion`) — Accordion + AccordionItem + AccordionTrigger + AccordionContent                        | Our markup + CSS — this component now owns the disclosure convention (rotating chevron, up when open / down when collapsed — symmetric, so no RTL mirror; hairline item dividers), which `SidePanelSection`'s collapsible mode composes. **Inline-flush by design**: trigger label and content carry no inline inset, so they align with whatever surrounds the accordion in its host (focus ring sits outside, positive offset); block padding stays for standalone row rhythm. Item `value`s are **semantic keys** (fixed kebab-case identifiers, the `TabDef.value` identity/label split — never display copy), the source of the emitted testids. `useAccordionItemExpanded()` (a thin wrapper over Kobalte's own `useCollapsibleContext().isOpen`) lets a trigger's own label swap between full/preview content depending on open state                                                                                                                                                                                                                                                   | **Kobalte Accordion** — the W3C APG accordion pattern: heading/button roles, `aria-expanded`, per-item id wiring, tedious to hand-roll and WCAG 2.2-graded. Audited against every `Accordion` usage in the current (MUI) app — see `kdd/` history for the full behaviour audit — and covers: a **shared single-open group** (`collapsible`, one shared `value` across items — e.g. a page of permission-gated settings sections), an **independent list** (`multiple`, one shared root wrapping a `<For>` so items toggle independently), and **closed-state preview labels** via `useAccordionItemExpanded()`. Deliberately excludes the old app's JsonForms array-editor accordion pattern (a different, undecided subsystem) and its one-off single-instance "card" variant (that need is now served by consuming `Accordion` directly as a standalone single item, as `SidePanelSection`'s own `collapsible` mode does). Content is unmounted while closed (Kobalte's `shouldMount`), animated via its measured `--kb-accordion-content-height` custom property. **Known gap (verified 2026-07-21 against @kobalte/core 0.13.12, reproduced with a bare unwrapped `Root` — an upstream library limitation, not fixable in this wrapper):** Up/Down/Home/End roving nav between triggers doesn't move focus; Tab-order navigation and Enter/Space activation on each trigger both work correctly, so basic keyboard operability is unaffected. Demoed at `#/showcase/accordion` (Components).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Table** (`ui/elements/table/Table`) — display shell                                                                                      | Hand-rolled (scroll-wrap `<div>` + real semantic `<table>`; page composes `<thead>`/`<tbody>`; cell/row variants are **data attributes** — `data-numeric`, `data-check`, `data-muted`, `data-mono`, `data-selected`, `data-row-link`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — **Display only**: the app's row look (toolbar-grey header row, hairline dividers, hover + selection tints) with no sorting/pagination/column state, no toolbar and no cell presets. Its registered role is the **static sub-table inside another surface** — a short, fixed row set in a `DetailCard` or a dialog (the items Variants tab's packaging + bundling lists), where `DataTable`'s toolbar chrome would outweigh the content. A screen's OWN row set is a `DataTable`, however few rows it holds: the moment it wants sorting, filtering, pagination, selection or resizing this shell is the wrong tool. `data-row-link` is a real `<button>` in the first data cell, so opening a row is keyboard-operable (the current app's click-only rows aren't). Selection tint never stands alone — the row checkbox carries the state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **DataTable** (`ui/elements/table/DataTable`) — the table engine                                                                           | Built by the integration branch on **TanStack Table** (`@tanstack/solid-table`) over the semantic-table conventions: sorting, column sizing, selection (select-all with the native `indeterminate` state), config persistence (`tableConfig.ts` + the `clickOutside` util); row presentation hooks — `rowDimmed` (data-dimmed → opacity, read-only records) and `rowTone` (semantic data-tone → palette tokens, e.g. 'info' for rows awaiting an action). Since the ui-standards § tables Phase 1 (2026-07-23) the table is the **composition root of its own chrome**: a toolbar (page-composed `filters` slot inline-start; exactly three controls inline-end — the Columns popover `ColumnSettings`, the Settings popover `TableSettings`, full-screen; the card/table view switch returns with Phase 3), row density via `viewDensity` config (`data-density` → 40/52/64px rows, Comfortable default), and ONE footer bar (the reused `ContentFooter`: the pager by default ⇄ "N selected" + the page's `selectionActions` + Clear while rows are selected — nothing floats over the rows) | **TanStack Table headless core** — principle #4's planned buy, landed via the integration branch (2026-07-09 merge). Imported as-built; reconciling it with the hand-rolled `Table` display shell (shared cell/data-attribute conventions) is a recorded follow-up. Live in the stocktakes list. **Card view** via `viewMode` + `meta.card` (one column set, two renderings — see [`kdd/table-state`](../../../kdd/table-state/draft-kdd.md)). **Grouping / tabs** via a typed `Column<T,K,G>.tabsAndGroups` + a `TabAndGroup<G>` list (one `tabsAndGroups` prop; the table owns the active tab internally): a tab strip in table view, one **group-row per group** in card view, both from one list. A _secondary_ column filter is baked in (separate from the persisted `columnVisibility`). A column tags itself with tab/group keys, or the **`ALL_TABS`** sentinel — explicit for anchors (e.g. batch) that appear in **every tab** but are **not** a card group (in card view they fall to the ungrouped row if they have no `card` region). The foundation for the shared line-edit modal ([`kdd/edit-line-card-table`](../../../kdd/edit-line-card-table/draft-kdd.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Pagination** (`ui/elements/table/Pagination`)                                                                                            | Built by the integration branch (hand-rolled buttons + page state over tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — Imported as-built at the 2026-07-09 merge; pairs with DataTable in the stocktakes list. Two faces: the default **stable-chrome** bar (rendered at any row count, "0–0 of 0" over an empty table), and — with `conditional` — the **earned footer** of spec/ui-standards § tables → pagination (D102): it exists only past one page, and below that nothing renders and no band is left, so the height goes to the rows. Suppression is a shared decision, since the host draws the band: `paginationState` (its own module, so the rule unit-tests without the Select/Kobalte graph) is what DataTable reads to drop the band with the pager. `fitContent` drops the table's own scroll box in CARD view so it lays out at content height and the HOST scrolls it — for a modal that wants the rows and the messages beneath them to scroll as one (the outbound line editor). Row view keeps the box regardless: it scrolls horizontally in that same element, and CSS cannot give one axis `auto` while the other is `visible`. The bar is the **pager alone** — the row total reads in the table's toolbar (`table-row-count`), the page size in its settings popover — and it renders only past one page, for every table, with no opt-in. The pager itself is a numbered window with an ellipsis at each break; pressing an ellipsis turns it into a box that jumps to the page typed in. `inBar` is for a pager that shares someone else's bar — a detail view's status footer hosts it there instead of giving it a band, so it sizes to its cluster rather than claiming the free space. Adopted by inbound shipments, outbound shipments and stocktakes: the lists keep the table footer, the detail views host it in the status bar.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **TableSettings** (`ui/elements/table/TableSettings`) — the ⚙ popover panel                                                                | Hand-rolled (the spec's settings popover: the Density radio — Comfortable ⭐ / Compact / Spacious, writing `viewDensity` via setConfig — a divider, the single "Reset table to default" action — visible but disabled while the layout is at its default — plus the central-admin save-as-global-default, relocated from ColumnSettings)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — Split from ColumnSettings per ui-standards § tables → column management (two toolbar controls: the per-column Columns panel, the table-wide Settings panel). Reset clears the user config layer for the current band via `setConfig(key, undefined)` — deliberately NOT TanStack's `reset*`, which would write empty state into the user layer and shadow a page's default hidden columns. Density is live (row heights 52/40/64px via `data-density`); the per-density padding/typography polish lands in Phase 2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **BooleanCell** (`ui/elements/table/BooleanCell`) — boolean table cell                                                                     | Hand-rolled: `<Switch>` over a `display` prop + CSS module + `CheckIcon`, with a `getBooleanCell(args)` fragment in the tableHelpers idiom to spread into a `Column` (accessor → boolean)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | — Pure CSS + one icon (own the simple). One component, three displays: `dot` (default — the reference app's glyph, kept as-is), `check` (a tick, e.g. an approved flag), `yesNo` (Yes/No text for both states, e.g. the stocktakes Locked column). The dot/check marker shows only when set (blank otherwise) and carries the set state's accessible name via `role="img"` + `aria-label`, never an `aria-hidden` glyph alone (ui-standards § assistive-tech parity) — the a11y improvement over the reference app's silent glyphs. Replaces the yes/no `getBooleanCell` that lived in `tableHelpers`. Consumers: the stocktakes list Locked column (`yesNo`); the items list's custom-field boolean columns (`dot`). Spec registry "boolean cell (flag in a table)".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **ChipListCell** (`ui/elements/table/ChipListCell`) — chip-list table cell                                                                 | Hand-rolled: a flex row of styled `<span>` chips + CSS module, with a `getChipListCell` fragment in the tableHelpers idiom to spread into a `Column`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | — Pure CSS (own the simple). Single line: overflowing chips clip at the cell edge, the full list rides the container title (ui-standards § tables truncation); empty array renders a blank cell. First consumer: the stock list's Master lists column (spec registry "chip-list cell").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **ProportionCell** (`ui/elements/table/ProportionCell`) — proportion / fullness table cell                                                 | Hand-rolled: a track `<div>` + a fill `<div>` (inline `inline-size` = the data) + CSS module, the percentage as a sibling `<span>`, with a `getProportionCell` fragment in the tableHelpers idiom (also reachable as `getCellDefinition('volumeUsed')`, which adds the width preset); the maths is the pure `proportionFill.ts` (unit-tested)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | — Pure CSS (own the simple), and deliberately **not** the platform's `<progress>` nor `role="progressbar"`: both clamp at their maximum, and over capacity (>100%) is exactly the state that must show — see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md). The fill is `aria-hidden`; the figure beside it carries the whole reading and the near- (>80%) / over-capacity tone states itself in visually-hidden text, so nothing rests on colour alone. Fill direction is the writing mode (logical properties → RTL free); the over-capacity fill also stripes, so the state survives without colour. Blank where there is no proportion (a capacity of `0` divides to Infinity/NaN); a real `0` draws an empty bar. Consumer: the locations list's Volume used column (spec registry "proportion cell (fullness bar)"; #740).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **EmptyState** (`ui/elements/feedback/EmptyState`)                                                                                         | Hand-rolled (centred sprout `NothingHereIcon` + bold "Nothing here" heading + message `<p>` + optional call-to-action children, filling its container; `graphic={false}` drops the illustration/heading for a plain message)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | — The current app's NothingHere: the sprout empty state for unbuilt pages, empty/filtered-to-nothing tables (via DataTable's `empty`), empty tabs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Spinner** (`ui/elements/feedback/Spinner`)                                                                                               | Hand-rolled, pure CSS (a spinning ring — same construction + keyframes as the Button spinner, factored out; `currentColor`, `sizeRem` prop, `center` fills+centres its container, `role=status` + label)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — No interaction/a11y contract to buy beyond `role=status`. The general-purpose loading indicator: centred in the DataTable's initial-load state (issues #160/#196) and the detail-view Suspense fallback. `prefers-reduced-motion` slows rather than stops it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **StatusChip** (`ui/elements/feedback/StatusChip`)                                                                                         | Hand-rolled, pure CSS (dot + label on a `color-mix` pill tint; one `colour` prop — always a `--status-*` token — drives both)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | — No interaction or a11y contract to buy. The label keeps `--text-body` for AA in both themes; colour never carries meaning alone. The `--status-*` contract tokens (lifted dark overrides; seven incl. the integration branch's `--status-finalised`) replace the prototype's hard-coded hex map, per the no-colour-literals rule. Live in the stocktakes list's status column.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Alert** (`ui/elements/feedback/Alert`)                                                                                                   | Hand-rolled, one `<div>` + CSS (`data-severity` → icon + `--alert-colour`; `role="alert"` as in MUI, so dynamically-appearing alerts announce)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — No library (the current app wraps **MUI Alert**; a static message panel has no contract to buy). Matches its rendered look — pale tinted panel, its 10px-radius override, severity-coloured icon, tinted text — with MUI's `lighten()`/`darken()` replaced by two `color-mix` rules over themed tokens (panel = severity over `--bg-white`, text = severity into `--text-body`), so dark mode needs no extra rules and text holds AA in both themes (worst case ≈7:1). Severity tokens: `--error-main` existed; `--info-main` aliases the action blue (theme.ts `palette.info`), and `--warning-main`/`--success-main` are **MUI's defaults, which the app's theme doesn't override** — its alerts render exactly these (nb: `--color-warning` is the unrelated cceStatus amber). Icons mirror the app's choices per severity (triangle / circle-i / check; error = the MUI built-in exclamation-circle it falls through to, added as `AlertCircleIcon`); an `icon` prop overrides the glyph by intent (the sync modal's completed check). `neutral` severity is the app's untinted drawer-grey notice (its last-successful-sync banner): `--bg-drawer` panel, body text, grey icon. The icon is vertically centred against the message (`align-items: center`) so it sits in the middle when the message wraps to several lines (Carl 2026-07-27; was top-aligned beside the first line to match MUI's geometry — panel height unchanged). A **`compact`** prop (the ui-standards `fb-banner--compact`) shrinks the same alert to an inline chip for a page-header meta strip carrying persistent, low-urgency context: `inline-size: fit-content`, tighter type/gap/padding (13px / `--weight-medium` / `0.375rem 0.625rem`) and a 15px glyph — same severity/tint/icon, smaller footprint. **Deliberate deviation from the spec's `white-space: nowrap` (Carl):** it's a single line while it fits and **wraps** once it hits the container, rather than overflowing a narrow screen. Not for errors or anything actionable — those keep the full width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **StatusMarker** (`ui/elements/feedback/StatusMarker`)                                                                                     | Hand-rolled, one `<span>` + CSS (`data-severity` → `color` from the shared feedback tokens; `role="img"` + required `aria-label`; per-severity default glyph via `Dynamic`, `icon` override)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | — Alert's severity vocabulary shrunk to a bare inline glyph beside a value (a cell's excess-request warning, an option row's emergency flag) where even the compact Alert chip is too heavy. Exists to carry the a11y contract a bare toned icon can't: the icon set renders `aria-hidden`, so hand-rolled markers were silent to AT and colour-only — the marker requires its MEANING as `label` and announces it (the BooleanCell pattern), and each severity keeps a distinct default glyph. Live in the internal-orders + requisitions line tables and create-modal order-type options; demoed on the Feedback showcase (Alerts card).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **StatusBadge** (`ui/elements/feedback/StatusBadge`) — inline row-status word chip                                                         | Hand-rolled, one `<span>` + CSS (`data-tone`/`data-appearance` → `color-mix` tints; Badge's AA colour recipe); optional decorative icon slot. The org standard's row-status badge (table interaction) — "On hold" / "Expired" / "Near expiry" / "Unallocated" beside a name; `outline` is the dashed not-yet-filled look                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Badge** (`ui/elements/feedback/Badge`)                                                                                                   | Hand-rolled, one `<span>` + CSS (`data-tone` → `color-mix` tints over themed tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | — No contract to buy: the small count/status pill riding on another element (the current app's MUI Badge role). Meaning is the label text plus the host's `title`; the semantic `tone` (neutral/warning/error) only escalates. Warning/error reuse **Alert's recipe** (severity tint over `--bg-white`, severity into `--text-body`) instead of the current app's solid MUI fills — at `--text-xs` no text colour holds AA on the solid severities (white on the light error red ≈4.3:1), the recorded AA-over-fidelity trade. `line-height: 1` + grid centring so the glyphs sit centred in the 1.4em pill regardless of the inherited body line-height.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **ProgressList** — determinate sync steps (`ui/sync/ProgressList`)                                                                         | Hand-rolled (`<ol>` + CSS Modules + tokens) + the library's own native-`Popover` for the completed-step count bubble                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | — Nothing to buy: the sync surfaces' phase list, rendered as a **horizontal stepper** — a circled per-step icon marker (passed by intent as a `Component<IconProps>`; an icon-less step shows its 1-based number, the current app's prepare step) joined by connector lines, label beneath, the in-flight marker pulsing (reduced-motion honoured), the failed step flagged via `error` (alert glyph in the error colour). **One count, on the in-flight step**: the done/total count renders beneath the in-flight step only, and that step's marker border doubles as a determinate **progress ring** (two half-arc border-box conics over the pale track, under a padding-box cover — the filled tone floods from the incoming connector toward the far side, top and bottom halves in step, so the fill front moves with the reading direction and meets the outgoing connector at 100%; mirrored in RTL), and a connector that newly fills plays a one-shot lightened sweep along its length (`--motion-sweep`, zeroed under reduced motion) as the fill arrives; the in-flight step's **elapsed time** ticks beneath its count (from `startedAt`/`finishedAt`, a 1s clock running only while a step runs; on `error` it freezes at the failure point, as the current app's does — and is omitted when the component mounted only after the failure, the failure moment being unknowable then); a completed step's final count and duration are a hover/focus `Popover` on its marker (which becomes a button — both also ride its accessible name, the `label.step-progress`/`-elapsed` keys; a progression-completed step with no finish stamp shows no duration); step columns stay equal-width whatever the count's length (issue #971). Connectors fill up to the in-flight marker; the run-out to pending steps stays pale (the primary variant's pale is a `color-mix` tint of the brand tone — the palette's own light shade is too close to main to read as unfilled). Completion derives from **progression** (everything before the furthest-started step is complete even without its own finished timestamp — the current app's #12172 fix). Semantic tone `variant`: `primary` (the sync modal) / `secondary` (the initialisation screen, whose text stays at the action blue while circles/connectors go pale). Steps are position-keyed (`<Index>`) and update in place because consumers rebuild step objects every status tick (kdd/solid-reactivity-pitfalls); the in-flight step carries `aria-current="step"`, with visually-hidden status text (pending / in progress / done / error) carrying the meaning — never colour alone; the status derives from the same progression state as the styling, so a progression-completed step announces done and the failure point announces error (the alert glyph is aria-hidden). Per-step status strings resolve via `t()` (recorded intl exception). Lives under `ui/sync/` — sync-specific by content, shared by the sync modal and initialisation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Dialog + ConfirmDialog** (`ui/elements/feedback/Dialog`, `ConfirmDialog`)                                                                | Hand-rolled over the **native `<dialog>` + `showModal()`** — controlled `open`/`onClose` via one `createEffect`; our markup/CSS on `--surface-raised`, `::backdrop` takes the scrim token                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | — **No library** (the prototype bought Radix Dialog; the platform supplies that contract now — see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md)): top layer + inert page = real focus trap, focus restore to trigger, Escape, `aria-modal`. We add scrim-click dismiss (an event targeting the dialog box = backdrop hit; padding lives on an inner wrapper so content clicks can't match — and the dismiss fires on **`click`**, with pointerdown+pointerup+click all required to have hit the backdrop, so it can't close mid-gesture and click-through onto the page behind on touch; see the note in `Dialog.tsx`), a `body:has(dialog:modal)` scroll lock, and `aria-labelledby`/`-describedby` wiring. **House chrome (#837, 2026-08-17):** every dialog draws the same frame with nothing to pass — a full-bleed hairline under the heading and another above the actions row, and buttons grouped at the **inline-end** (dismiss first, emphasised confirm last). What the error modal introduced as two opt-in props is now what a `Dialog` simply is, so no call site re-decides it; `titleHidden` drops the heading rule with the heading and `chromeless` drops both (a hairline needs a surface to divide). A `footer` banner sits ABOVE the actions hairline — it is the content's last word, not part of the button row. `ConfirmDialog` is the Cancel/confirm preset, built from the **StandardButtons** — `CancelButton` beside `OkButton` or (with `confirmAction="save"`) `DialogSaveButton`, so a save confirmation is literally the same control as a modal footer's Save. The confirm slot is always the dialog's **primary** action, including a bespoke `confirmLabel` verb (Discard, Delete); a destructive confirm instead passes `confirmVariant="danger"` (a plain danger `Button`, since a standard button owns its tone); Cancel stays secondary (Carl 2026-07-28 — same decision family as spec D55). Wired to Save in the ContentFooter showcase. `dismissable={false}` makes a dialog **blocking** (Escape swallowed via the `cancel` event, scrim clicks ignored — actions are the only way out), for the modals a user must answer; live in the app's re-login, store-selection and unexpected-error modals. `closeButton` renders a brand-toned icon-only dismiss pinned to the top corner (informational dialogs; suppressed when blocking); `titleHidden` keeps the required title as the accessible name but paints no heading — both for the current app's heading-less sync modal (2026-07-09 boot-screen sweep, which retired the integration branch's `Modal` and `FormField` — see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md)). **Full-screen on phones (2026-07-17):** below the compact breakpoint a `size="large"` (workbench) dialog fills the viewport edge to edge (overriding the UA's `max-inline/block-size` inset + the corner radius); smaller dialogs stay centred cards. Driven by `data-fullscreen`, which `Dialog` sets on a large dialog when `useIsCompact` matches, so the 600px cutoff stays once in `breakpoints.ts` (a CSS media query can't read a custom property). The two WORKBENCH sizes — `size="large"` (a centred card at its working width, 56rem by default, `widthRem` for a bigger card) and `size="full"` (`100vw - 4rem`, for a line table too wide to be a card at any number — the shipment + stocktake editors at ~20 columns; DESIGN_STANDARDS ledger). Both give a height elastic between ~60vh and ~80vh, past which the body flexes so a tall child scrolls internally with header/footer/actions pinned (#771 — whose empty space was vertical, which is what the height band answers) is demoed in the Dialog / Modal showcase. **Initial focus** lands on the dialog panel (`tabindex=-1` + `autofocus` on the inner body), so an autocomplete never pops its listbox open just because a dialog appeared — this beats any `autofocus` a caller puts on a field, being first in tree order. A dialog that exists to capture one thing declares `initialFocus={target}` instead, a `createFocusTarget()` handle the Dialog fires after `showModal()` — see [`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md). Width comes from the shared content measures (`width="prose | form            | wide"`→`--measure-*`), the same vocabulary `ContentContainer`uses, with`widthRem`kept for a bespoke width — the frame owns its width, so capping the content inside instead would leave the title + actions rows wide around a floating body. A measure (like`size="large"`) also opts into the full-screen sheet below the navOverlay line; a default / `widthRem` dialog stays a centred card. |
| **Popover** (`ui/elements/feedback/Popover`)                                                                                               | Hand-rolled over the **native Popover API** (`popover="auto"` panel + `popovertarget` invoker button); placement is our measured geometry — preferred side / logical alignment, flip when the viewport lacks room, edge clamp, re-place on scroll/resize                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — **No library** (the prototype bought Radix Popover — see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md)): top layer (no portal, escapes scroll-container clipping), light dismiss, Escape and invoker `aria-expanded` are platform-supplied (also set explicitly for older engines). Hand-measured placement because CSS anchor positioning is too newly Baseline; it migrates to pure CSS later with no API change. `openOnHover` opens it on pointer-enter/focus of the trigger (closing once the pointer leaves both trigger and panel, with a small grace delay) as well as click — for bubbles whose trigger IS the content (the StatusIndicator row) where hover reads more naturally; keyboard focus opens it too, so it isn't hover-only (a11y). For content bubbles (a row's comment, help text) — a _menu_ still buys Kobalte DropdownMenu. **`focusTarget`** binds a [`createFocusTarget()`](../utils/createFocusTarget.ts) handle to the trigger button — the trigger is constructed in here, so an owner that must focus (or synthetically open) it can't reach it with a plain ref ([`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Content footer** (`ui/layout/ContentFooter`) — ContentFooter + ContentFooterActions                                                      | Hand-rolled (one flat flex-wrap strip, `--bg-white` ground + hairline top border; ContentFooterActions pins itself inline-end via `margin-inline-start: auto`, mirroring HeaderButtons)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | — The pinned action bar between a page's scrolling body and the orange app footer (the current app's blue-buttons bar: History / Cancel / Save, or "N selected" + selection actions). **Pure layout, zero state** — same contract as Header: the page composes the content, owns every handler, and hands the bar to the Page frame's **`contentFooter` slot** so it never scrolls with the body. **Contextual content is composition, not a store**: the page swaps the children with a `<Show>` on its own selection signal — the prototype's zustand `selectionFooter` store is deliberately not carried over (see [`kdd/page-composition`](../../../kdd/page-composition/draft-kdd.md)). A plain `<div>`, not `<footer>` — the orange bar is the page's one footer landmark. Save opens the standard `ConfirmDialog` (wired in the showcase).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Login page** (`pages/Login`) — gradient hero + form panel                                                                                | Hand-rolled layout (flex 50/50 split; hero = gradient `<section>` with the strapline `<h1>`, form panel composes library TextField / Button / LanguageSelector under the MSupplyGuy logo)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | — Recreated from the current app's `LoginLayout` (see [`kdd/page-composition`](../../../kdd/page-composition/draft-kdd.md)): copy, 156° gradient, logo-width form column and bottom-left hero placement are from source; colours land as the `--gradient-primary` / `--bg-login` / `--login-hero-text` contract tokens. Hero type is fluid (`clamp()`), and the hero simply doesn't render < 600px (principle #7). Submit is disabled until both fields are filled and navigates to `#/home` (no auth yet); store selector, error panel and password-visibility toggle arrive with the auth/Feedback work. One AA-over-fidelity deviation: the version line is `--text-secondary`, not the original's failing `opacity: 0.4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Theme toggle** (sun/moon)                                                                                                                | Hand-rolled (`ui/elements/buttons/ThemeToggle`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | — Just a `<button>` (with `aria-pressed`) that flips `data-theme` on `<html>` and persists to `localStorage`; a pre-paint script in `index.html` prevents a light flash. The `[data-theme='dark']` token block in `tokens.css` recolours everything via the cascade.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Select** — styled drop-down (`ui/elements/selectors/Select`)                                                                             | Our markup + CSS over Kobalte parts (Trigger/Value/Content/Listbox/Item)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Kobalte Select** — options need rich content (status dot, two-line label) a native `<option>` can't hold, and the replacement is a full `role="listbox"` popup with typeahead + keyboard/focus semantics that WCAG 2.2 grades. Solid analogue of the prototype's Radix Select; styled via `data-*` state attributes. API mirrors the prototype (string `value` in/out). `required` puts TextField's asterisk on the label (+ `aria-required` on the trigger) so a select and a text field read alike in one form. **`focusTarget`** binds a [`createFocusTarget()`](../utils/createFocusTarget.ts) handle to the trigger button, for the same reason ([`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md)). **`clearable`** (+ `onClear`) puts a clear ✕ in its own slot beside the chevron while a value is selected — for an OPTIONAL pick (a report filter's enum argument); the trigger is a real `<button>`, so the ✕ is a sibling button overlaying a slot a spacer reserves in the trigger's flex row (same own-slot rationale as Combobox's clear). Clearing hands focus to the trigger — the ✕ removes itself, so left alone focus would fall to `<body>` (the same reason Combobox's clear refocuses its input). Requires controlled `value` — under `clearable` an absent value maps to Kobalte's null ("no selection") instead of flipping uncontrolled. **Empty option set:** Kobalte's `open()` early-returns on `options.length <= 0` and offers no `allowsEmptyCollection` (Combobox does), so a trigger with no options silently did nothing — we pass ONE disabled sentinel option carrying `label.no-options` instead, which satisfies the length check and renders as the greyed, unselectable row (#906); Combobox/MultiSelect get the same copy from their own status rows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Combobox** — autocomplete (`ui/elements/selectors/Combobox`)                                                                             | Our markup + CSS (search icon, hand-rolled clear button) over Kobalte parts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Kobalte Combobox** — THE buy-don't-build widget (`aria-activedescendant` virtual focus, SR announcements, arrow/Enter/Escape semantics). Replaces Downshift `useCombobox`; upgrade over the prototype: popup is portaled with collision-aware, control-width placement. We supply the (locale-aware, code-or-name) filter predicate. `required` puts TextField's asterisk on the label (+ `aria-required` on the input) so a lookup and a text field read alike in one form. **`focusTarget`** binds a [`createFocusTarget()`](../utils/createFocusTarget.ts) handle to the inner `<input>` — the supported way for an owner to focus this picker after an action, since the input is internal to the Kobalte composition ([`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md)). Takes TextField's **`width`** caps (compact/short/long/full) **and TextField's default**, `full` — so a picker stacked among text fields obeys the same measure with nothing passed. It defaulted to `long` (37.5rem) until 2026-08-13 "because option text runs long", which made it the one control whose bare width disagreed with every neighbour's; long option text is a POPUP problem, and the popup can already outgrow the trigger via `matchTriggerWidth={false}`. See [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md) § input widths. Kobalte does **not** virtualise its listbox and every option is a live component, so in client mode we mount at most **`maxVisibleOptions` (default 100) MATCHING options** — filtered first, so a search still reaches an option thousands of rows down — with a "keep typing to narrow" row reporting the rest, so a truncated list never just appears to end. Uncapped, a whole-store location picker (5,000 rows) mounted 43,000 DOM nodes and blocked a Lenovo tablet's main thread for 30 seconds on one tap. Raise it for a list meant to be scrolled end-to-end; server mode ignores it (the caller pages via `onReachEnd`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **AsyncCombobox** — server-paginated autocomplete (`ui/elements/selectors/AsyncCombobox`)                                                  | Composes **Combobox** with the paginated-search loop (`ui/utils/createPaginatedSearch`) — no new markup, just the server-fed wiring                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **None beyond Combobox's Kobalte.** Adds the server behaviour Combobox exposes as hooks: typing refetches via the caller's `fetchPage`, scroll-to-bottom loads the next page, the first fetch is deferred to the first open (`eager:false` + `ensure`), and a pre-set `selected` is seeded into the list so its label shows before its page loads. The engine the domain pickers (`ItemSearch`, `NameSearch`) bind to — each supplies only a `fetchPage` + option row. Passes `required`, `width` and **`matchTriggerWidth`** straight through to Combobox — the last of those added 2026-08-14 so a server-backed picker can size its popup to its content like the location pickers do (the patient picker's code + date of birth + name had nowhere to go in a dialog-column-width popup, #1041).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **MultiSelect** — multi-select autocomplete (`ui/elements/selectors/MultiSelect`)                                                          | Our markup + CSS (tags rendered via Kobalte's Control render-prop state) over Kobalte parts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Kobalte Combobox `multiple`** — replaces Downshift `useMultipleSelection`+`useCombobox`. Backspace removes the last tag, menu stays open across picks (Kobalte defaults). Delta from the prototype: picked items stay in the list check-marked (click to deselect) instead of dropping out — see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md). Takes TextField's **`width`** caps and its `full` default (added 2026-08-13 — it was hardwired to the `long` cap with no prop at all, so a caller couldn't correct it). **Keeps Combobox's interaction contract, not Kobalte's defaults:** `triggerMode="focus"` (Kobalte defaults to `"input"`, which opens only once the user TYPES — so clicking the field did nothing and the chevron was the only pointer way in), plus the dialog pair Combobox carries — `Portal mount` via `usePortalMount` and `onInteractOutside={keepPopupOpenOnInsideContent}`. Two sibling pickers that look identical must answer a click identically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **FilterBar** — "Filters" dropdown + inline filter chips (`ui/elements/selectors/FilterBar`)                                               | Our markup + CSS (bordered select-style triggers, text chip with search icon, count badge, round remove buttons) over Kobalte DropdownMenu ×2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Kobalte DropdownMenu** — same buy as SplitButton/LanguageSelector (menu focus management, typeahead, arrow/Escape). The current app's FilterMenu model, ported from the prototype's Radix FilterBar: the menu lists available fields, picking one adds an editor chip — a text input (`FilterTextInput`), a number box (`FilterNumberInput`, over the shared `NumberField`) or a number range (`FilterNumberRange`, two `NumberField`s From/To through one merged debounce; no wire range operator exists, so the vertical maps `{ from, to }` onto its own keys), a native date input (`FilterDate`, calendar icon; ISO value), a date-only range (`FilterDateRange`, over `DateRangeField`) or a date-_time_ range (`FilterDateTimeRange`, two independent `DateTimeField`s — no shared range primitive picks time yet, so this composes two whole fields rather than extending `DateRangeField`; first used by the items Ledger tab), or a multi-check enum menu (`CheckboxItem` + `ItemIndicator`, `closeOnSelect={false}` so several tick in a row), plus a bar-level "Clear all" that removes every chip. **The text chip debounces** (spec: ui-standards/inputs.md § Server-bound input — the date and enum chips are discrete choices and apply immediately): keystrokes render in a local draft immediately, but `onInput` — and so the query + URL — fires once, `debounceMs` (default 300ms) after typing pauses; Enter/blur flush at once, a draft pending at unmount is discarded (never resurrects a removed chip); `debounceMs={0}` opts a client-side-filtered consumer out (built on the shared `ui/utils/createDebounced`). The filter object is generic over `F` and needn't be a GraphQL shape — a `FilterGroup` (`extra`) hosts a second, differently-typed group (custom-field filters) in the same bar. **The filter object is a controlled prop** (`filter`/`onChange` — parent-owned, and URL-backed in the real verticals, so a filtered view is shareable); **chip visibility lives in that object**, not in a local signal — a chip shows iff its key is present (present-as-`null` = added but empty, which the page's `stripEmpty` drops before querying) — see [`kdd/page-composition`](../../../kdd/page-composition/draft-kdd.md). A **default filter** ([ui-standards › tables § filtering](../../../spec/ui-standards/tables.md#filtering)) is nothing but a key the page **seeds** present-as-`null` in the filter state it starts with (`DEFAULT_STATE` / `DEFAULT_URL_STATE`): its chip is on the bar from the first render and is an ordinary chip from there — its ✕ removes it, "Clear all" takes it off with the rest, and the menu offers it back (#563; there is deliberately no flag on the definition). Which chips show, which the menu still offers, and whether "Clear all" appears are pure functions in `filterBarLogic.ts` (unit-tested — no DOM harness exists) that `groupOps` calls. **Picking a field hands its new chip the next action**: a `createFocusTargets()` registry keyed by FILTER KEY (never a test id) holds each chip's destination, the chip chrome offers it over its subtree, and every `Filter*` control **claims it itself** — so a filter definition made of them writes no focus wiring, and on menu-close the bar focuses a typing editor or opens a button editor's chooser. A definition composing a control from outside this file binds the `focusTarget` render-prop by hand; a chip with two focusables wraps its later slot in **`NotChipEditor`** so the caret lands on the first ([`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **StoreSelector** — searchable store-picker panel (`ui/elements/selectors/StoreSelector`)                                                  | Hand-rolled, composed from library parts: card-row `role="listbox"` of `role="option"` rows + `StatusChip` Default/Last-used markers + a `TextField` search (7+ stores only) + the always-open `Checkbox` at the top                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | — No new primitive: it's a **composition** ("own the simple"). Styled after the current app's login store-selector. Presentational + dismiss-agnostic — takes the (pre-ordered) stores + `defaultStoreId`/`lastUsedStoreId`/`pinnedCount`, reports the chosen id and the checkbox state via `onConfirm(id, alwaysOpen)`; the host places it — the app's routed store-selection screen (`src/store/StoreSelectionScreen`, in the login frame) and the store-switch modal (`src/store/StoreSwitchModal`, a Dialog whose heading carries the title, so it passes `hideTitle` to skip the panel's own h2 — spec startup S3's two hosts, D14). Clicking a row confirms it and enters — no Continue, no follow-up prompt (D113, issue #193); arrows move the highlight and Enter confirms it; the highlight falls back default → last-used → first so Enter always has a target. The always-open checkbox (spec startup SL-9) sits at the top, seeded from the device's saved opt-in via `defaultAlwaysOpen` (false on a fresh device) — ticked, the store picked next is saved; every toggle is also reported at once via `onAlwaysOpenChange`, from which the host clears any saved store on an untick, pick or no pick. Active row = token tint **and** `aria-selected` (not colour alone); markers are StatusChips (dot + label). Demoed at `#/showcase/dialog` (Dialog / Modal), filling a blocking dialog as the app's store login does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **ColourTag** — record colour tag: dot + swatch picker (`ui/elements/selectors/ColourTag`)                                                 | Hand-rolled (`ColourTagDot` passive dot; `ColourTagPicker` composes the library **Popover** — trigger dot / empty dashed ring, panel of round swatch `<button>`s)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | — The current app's ColorSelectButton/ColorMenu role (a user-set colour on a record for visual grouping). Colour VALUES are data persisted on the record, carried via a `--tag-colour` custom property like StatusChip — no colour literals in the CSS; the palette is `TAG_COLOURS`, baked into the component (one app-wide set, hex-for-hex the current app's ColorMenu defaultColors so tags agree across both apps; labels via `colour.*` locale keys, trigger label defaults to `colour.label`). Swatches are labelled by colour name (never colour alone); selection reads as a ring gap; both pieces swallow click bubbling so tagging inside a clickable table row doesn't open the row. Closing on pick uses Popover's `closeOnClickInside` (light dismiss only fires outside a `[popover]`). Two contexts via semantic `variant`: `'row'` (inline in a table cell) and `'field'` (the side panel's standalone editable field — same dot, but the trigger grows an invisible 24px-minimum hit area, the full touch target on coarse pointers; `placement` pass-through for the panel edge). No `disabled` prop: read-only is composition — render `ColourTagDot` instead of the picker; its `colour` also takes `null`, rendering an invisible spacer that holds the dot's width so names align across tagged and untagged rows (the dashed ring stays picker-only — it would falsely advertise the affordance). Sitting beside text is the component's job, so it owns the ergonomics: `vertical-align: middle` (inline boxes otherwise ride the baseline) and a logical inline-end gap before the text. Live in the internal-orders list (Draft rows); demoed in the Selectors showcase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Button** — action button (`ui/elements/buttons/Button`)                                                                                  | Hand-rolled (plain `<button>` + CSS Modules + tokens; `createRipple`/`Ripple` for the click ripple)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | — Nothing to buy: it's a native `<button>`, so role + accessible name + keyboard/Enter/Space + `disabled` come free. White pill, no border, shadow[2]; `variant` picks the tone — **`primary`** (brand, default) or **`secondary`** (action, footer edits), mapped to palette tokens by the CSS via `data-variant` (**variant APIs are named semantically, never after colours** — renamed from `color="orange"/"blue"`, Carl 2026-07-09); fills with its tone on hover (label + icon → white). `iconPosition="end"` puts the icon after the label (login's arrow) — a CSS `order` flip, so AT reading order is untouched. Faithful Solid port of the RnD prototype's `<Button>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **IconButton** — icon-only control (`ui/elements/buttons/IconButton`)                                                                      | Hand-rolled (plain `<button>` + CSS Modules + tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — The compact, icon-only sibling of Button, for where a labelled pill is too big: table toolbar controls, table row actions (duplicate/delete), panel actions. Native `<button>` (keyboard/Enter/Space/disabled free); a **`label` is required** (no visible text) and drives `aria-label` + hover `title`. `bordered` = an outlined box (input-edge border + white fill, reads as a button in a row); bare otherwise (hover-tinted icon). `variant` (`neutral` default / `danger` for destructive) + `size` (`small` dense rows / `medium`), via `data-*` mapped to palette tokens. Consolidates the ad-hoc icon buttons (table `controlButton`, ColumnSettings `iconButton`, ThemeToggle) — those can migrate to it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **SplitButton** — action + dropdown caret (`ui/elements/buttons/SplitButton`)                                                              | Our markup + CSS (two `<button>` halves, 1px divider, per-half ripples) over Kobalte DropdownMenu (Trigger/Portal/Content/Item)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Kobalte DropdownMenu** — the caret opens a menu popup whose focus management + type-ahead + arrow/Escape nav are the dangerous-to-hand-roll part (same call as the footer LanguageSelector, 2026-07-08). The main half runs the selected action; picking a menu item selects **and** runs it (the app's SplitButton/ExportSelector model). Caret open state styled via Kobalte's `data-expanded`; menu portaled to `<body>` so it inherits `dir` for RTL. Solid port of the prototype's ExportButton, generalised. Carries the `<Button>` async contract too — `loading` swaps the main half's icon for a spinner and makes BOTH halves inert (`aria-busy`), and `mainLabel` overrides the main half's visible label without touching the menu, which together give the in-place outcome report the action-feedback rule requires (`spec/ui-standards/controls.md` § action feedback — never a toast). The main half is `aria-live="polite"` so the swap is announced. `domain/reportFiles/ListExportAction` is the reference consumer. Also carries `<Button>`'s `collapsible` contract (shared `buttons/collapsible.ts`, so the tiers can't drift): the MAIN half sheds its label to its icon at the phone (`true`) or narrow-viewport (`'narrow'`) tier while the caret keeps its fully-labelled menu, and the hidden label doubles as the tooltip — the detail-header Add buttons in inbound shipments / internal orders / requisitions are the reference consumers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **DisclosureToggle** — "Show / Hide advanced options" form disclosure (`ui/elements/buttons/DisclosureToggle`)                             | Composed: a ghost `Button` + `ChevronDownIcon` + two CSS rules for the rotation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | — Nothing bought: `Button` already owns the tone, ripple, sizes and disabled contract, so this adds only the disclosure semantics (`aria-expanded`, optional `aria-controls`) and the chevron that rotates 180° to the state it will move to — the same accordion convention as `Accordion`/`SidePanel` (up/down, never RTL-mirrored), its duration a `--motion-*` token. Caller-owned `expanded`: the control holds no state and does not own the revealed region, which the caller renders in its own JSX (explicit composition) — that is the line against `Accordion`, which owns a titled section's header row AND its panel. Consumers: the initialisation form and the Synchronisation settings form, both revealing the optional sync batch size.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **CheckboxButton** — pill button with a checkbox inside (`ui/elements/buttons/CheckboxButton`)                                             | Hand-rolled (a `<label>` pill wrapping a visually-hidden **native `<input type="checkbox">`** + a styled square indicator + `CheckIcon` + label)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | — The whole pill is the click target; it IS a real checkbox (native input — semantics, Space toggling, and the e2e contract's `input[type=checkbox]` hook come for free; focus ring drawn on the pill via `:has()`). Caller owns `checked`, flips it in `onChange(next)`. The square indicator fills with `--primary-main` + reveals the check when checked; outlined pill otherwise. Open mSupply's on-hold control — live in the stocktake detail footer. (Replaced an earlier ToggleButton, which was a filled-pill toggle; OMS's control is a checkbox-in-a-button, so this is the shape.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **StandardButtons** — pre-composed common actions (`ui/elements/buttons/StandardButtons`)                                                  | Hand-rolled: thin wrappers over `<Button>` (no new primitive), labels from the shared intl catalog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — Not a new element — a set of **named presets** for the actions on nearly every dialog/form, so callers stop re-picking tone + label: `OkButton` (primary, a not-a-save dialog confirm — `ConfirmDialog`'s default), `CancelButton` (secondary), `SaveButton` (primary + `SaveIcon`, `collapsible` on by default — a page/toolbar Save), `CloseButton` (secondary + `XCircleIcon`, `collapsible` on by default — the **action-footer** dismiss on a record screen, beside the status control; labelled because a footer action reads as a verb, with the icon-only form rejected — it was hand-rolled two ways across five verticals before this preset, Carl 2026-07-30), `DialogSaveButton` (primary, icon-less — the dialog-footer Save), `SaveAndNextButton` (primary, icon-less). Each fixes only its identity — `variant`/`icon`/`children` are `Omit`ted from its props; everything else a `Button` takes (`onClick`, `disabled`, `loading`, `size`, `type`, `class`, `aria-*`…) passes through. Labels are `t('button.ok'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 'button.cancel' | 'button.save'                                                                                                                                                                                                                                                                                                                                                                                    | 'button.save-and-next')` (`save-and-next`added to en/fr/ar), so they translate + stay consistent.`SaveButton`honours`collapsible={false}`. No barrel — imported by direct path (explicit composition). Drop to raw `<Button>`when a bespoke label/tone is needed. See`docs/DESIGN_STANDARDS.md`. |
| **CopyToClipboardButton** — the record copy action (`ui/elements/buttons/CopyToClipboardButton`)                                           | Hand-rolled: thin wrapper over `<Button>` (no new primitive) that owns the copy behaviour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The one implementation of `spec/ui-standards/controls.md` § copy to clipboard, used by every detail side panel that offers the action. The caller passes `load` — a supplier of the WHOLE record (a node it already holds, or its own unpaginated fetch where the row table is server-paged; `undefined` copies nothing) — and the button owns the rest: `JSON.stringify` with `indent` spaces (2, or a vertical's contracted width), the `navigator.clipboard` write, and the in-place outcome report (label + icon swap to _Copied to clipboard successfully_ / _Copy failed_ for 2s, `aria-live="polite"`, spinner while `load` is in flight, re-entry guarded) — never a toast, per controls § action feedback. Copy is a **read**, so there is deliberately no status/permission gate in its props. Fixes `variant`/`icon`/`children`/`loading`; `data-testid` defaults to the contract id `copy-to-clipboard-button` and everything else a `Button` takes passes through.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **StatusIndicator** — status progression + history (`ui/elements/feedback/StatusIndicator`)                                                | Hand-rolled, pure CSS (`<ol>` of chevron-separated stages; `data-reached`/`data-current`) + the `Popover` for history                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — The shared status control across detail views (stocktake, shipments…). Takes ordered `steps` (`{ label, date? }`) + a `current` index: reached (past) stages read `--text-body`, the current stage is `--primary-main` (bold), not-yet-reached are `--gray-light`; a CSS `::before` chevron separates them (RTL-safe). Hovering (or focusing) the WHOLE row opens a `Popover` (openOnHover) with the status history — a vertical dot-timeline (a filled/ringed dot per reached stage, connected by a line) with each stage's `localisedDate`. Meaning never by colour alone — position + label + the accent weight, and the popover spells out the timeline (AA). Replaced StatusCrumbs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Comment** — note behind an icon (`ui/elements/feedback/Comment`)                                                                         | Hand-rolled thin wrapper over the library **Popover** (`MessageSquareIcon` trigger + a bold heading over the body); one CSS module                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — The current app's **PopoverCell** (its comment-column control), ported to the native Popover. A quiet grey comment icon (brand on hover) that reveals the text on hover / focus — and on click / tap too, so it opens on touch where there's no hover — with a bold "Comment" heading (overridable `label`, defaults to `label.comment`). `openOnHover`, `placement` default `bottom-end` (matches the app's down-and-across bubble). Renders **nothing** when `comment` is nullish/empty — so a table cell drops it in unconditionally. Consumed via `getCommentCell` (a `tableHelpers` fragment, centred cell) in the stocktakes and inbound-shipments **list** comment columns; demoed in the Feedback showcase. Body preserves author line breaks (`white-space: pre-wrap`) and breaks long tokens (`overflow-wrap: anywhere`) rather than overflow the bubble.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **InfoTooltip** — help text behind an icon (`ui/elements/feedback/InfoTooltip`)                                                            | Hand-rolled thin wrapper over the library **Popover** (`InfoIcon` trigger + a text body); one CSS module                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — The help-text sibling of `Comment` (the current app's field/stat "?"/info tooltip): a quiet brand-toned info icon that reveals a short gloss on hover / focus, and on click / tap too (touch has no hover). `openOnHover`, `placement` default `top`; icon-only, so a `label` names the trigger (defaults to `label.more-information`). **`tone`** sets the marker's emphasis: the default quiet grey brightening to brand on hover (right beside a label, which carries the weight), or **`primary`** — the standing brand-toned mark the current app uses where the icon stands alone in a muted row (`Statistic`; app renders it `color="primary"`, flat). `primary` can't brighten further, so its hover state is a 12% tint of its own tone, the bare-icon-button treatment shared with `Dialog`'s close and `IconButton`'s danger variant; it carries `Comment`-style padding to give that tint a box. Its primary home is the **`labelInfo`** slot carried by **every labelled input and selector** (Carl 2026-07-29 — TextField and its wrappers NumberField / CurrencyField / PasswordField, TextArea, the four date/time fields via `FieldShell`, Checkbox, ToggleSwitch, RadioGroup, Select, Combobox / AsyncCombobox, MultiSelect): passed there it sits inline after the field's label, **always outside the label element** so its own name is never concatenated into the control's — on the self-labelling controls (Checkbox / ToggleSwitch) that also keeps a click on the icon from toggling the control, and on RadioGroup the `<legend>` has to hold it, so the fieldset is named by an explicit `aria-labelledby` on the label text alone. Ignored under `hideLabel` (no visible label to hang it off). The clean replacement for the inbound Currency tab's former inline-styled `<span title=…>` rate tooltip, and the place for a standing explanation that would drag a dense row taller as `helperText`. Also drops inline beside any term. Demoed in the Feedback showcase across every label shape. **Domain selector wrappers (`NameSearch`, `ItemSearch`, `ReasonSelect`, …) do not forward it** — same hand-threading gap as `size`; add it to the wrapper when a caller needs it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **ErrorDetails** — raw error / JSON behind a disclosure (`ui/elements/feedback/ErrorDetails`)                                              | Hand-rolled over the native `<details>`/`<summary>` disclosure (+ a scrolling `<pre>`); one CSS module                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | — The "show more / raw detail" expander a caller tucks inside an `<Alert severity="error">` so the message stays terse. `detail` (raw text / JSON → `<pre>`, wraps + scrolls, capped 10rem), optional `hint` above it, optional `summaryLabel` (default `error.more-info`). Own-the-simple: native `<details>` needs no JS — open/close, keyboard, and a11y are platform-supplied, so no library. De-dupes the identical block previously hand-rolled in the sync modal, initialisation screen, sync settings, and report detail. Demoed in the Feedback showcase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **ErrorDialog** — the blocking error modal (`ui/elements/feedback/ErrorDialog`)                                                            | Composes **Dialog** (native `<dialog>`) and passes nothing to get its chrome — the hairline above the actions and the inline-end buttons this dialog introduced (2026-08-13) are the house default for every dialog since #837, so the two props that used to carry them (`actionsDivider` / `actionsAlign`) are gone; Show details is a native `<details>` disclosure (own hairline above, rotating `›` chevron summary) over a monospace label/value grid; one CSS module                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | — One fixed wording per failure condition (`unreachable` / `timeout` / `server` / `unknown`), per ui-standards › error dialogs (spec D109, retiring "Oops! Something's gone wrong."): plain-words title + one guidance line, primary = the fix (Retry / Try again), Close dismisses in place, `onDashboard` renders a quiet ghost-button tertiary affordance at the actions row's inline-start (never a peer button). The raw cause appears only in the Show details support block — quotable timestamp reference, cause, store, request, share-with-your-administrator line — collapsed for a server error, open for the unmapped fallback, omitted for the self-explanatory conditions. `duringEdit` (a failed mutation) adds a success-toned reassurance line and drops the dashboard affordance. Opts out of Enter-to-confirm (a stray Enter must not reload or dismiss unseen); Escape = Close. The app's single consumer is the global unexpected-error modal (spec/startup S5); demoed per condition in the Dialog showcase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Ripple** — click ripple (`ui/utils/createRipple` + `Ripple`)                                                                             | Hand-rolled: `createRipple` primitive (signal of active ripples + `onPointerDown`) feeding a `<Ripple>` render layer; CSS keyframes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | — **The one deliberate JS-for-interaction exception** (everything else is pure CSS). The ripple must start at the exact pointer coordinates, which CSS can't read — so JS supplies each ripple's px position/size (dynamic pixel offsets, not layout spacing) and spawns one element per click; the animation stays CSS. Skipped under `prefers-reduced-motion`. Tint is the host's `--ripple-color` (set per button state via `color-mix`, so it tracks the theme). Solid port of the prototype's `useRipple`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Flash** — self-clearing outcome (`ui/utils/createFlash`)                                                                                 | Hand-rolled Solid primitive: a signal plus a superseding timer, auto-cancelled on cleanup                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | — Nothing to buy: it's a `setTimeout` with the three details that get forgotten when hand-rolled — a second `show()` restarts the clock instead of inheriting the first outcome's remaining time, `clear()` cancels rather than leaving a timer to fire at a reverted control, and disposal is registered only under an owner (the `createDebounced` ownership rule). Drives the in-place outcome report the action-feedback rule requires (`spec/ui-standards/controls.md` § action feedback, never a toast) — `CopyToClipboardButton` and `domain/reportFiles/ListExportAction` both run on it. Deliberately NOT a `Button` prop: which outcome to report is the action's semantics, not the control's — `saveBlob` resolves three ways and the middle one (a dismissed OS save picker) must report nothing, a call only the caller can make. The components own rendering busy/outcome (`loading`, `mainLabel`); this owns the timing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Text** — type primitive (`ui/elements/typography/Text`)                                                                                  | Hand-rolled (polymorphic element via `Dynamic` + CSS Modules + tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | — Nothing to buy: no interaction/a11y contract, just the app's four type styles — `body`/`bodySmall`/`heading`/`subtitle`, a Solid port of the current app's MUI `body1`/`body2`/`h6`/`subtitle1`. **Sets size + line-height + weight only, never colour** (`color: inherit` — the container decides, so a cell's subtext is secondary because the _cell_ sets it). Weights from existing showcase precedent: body 400 (`--weight-regular`), bodySmall 500, heading 700, subtitle 600 (`--weight-semibold`); subtitle is a _deck under a title_ — a fixed `--text-sm` step (a bit smaller than heading), kept rem not `em` since siblings can't size off each other. `variant` sets visual size; `level` sets heading rank independently (`<h2>`–`<h6>`) so size never dictates the document outline (WCAG 2.2). **Scope:** for areas that _compose_ a variable arrangement of text blocks (SidePanel, a cell's main+subtext); a single fixed role (Button label, table `th`) stays styled by its own component and takes raw text — see `TEXT_SPEC_DRAFT.md` and [`kdd/typography-text`](../../../kdd/typography-text/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **RecordLink** — kind-toned related-record link (`ui/elements/typography/RecordLink`)                                                      | Hand-rolled thin wrapper over the router's `<A>` (`@solidjs/router`) + CSS Modules + tokens (`data-kind` → tone only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — Nothing to buy: the one treatment for a link to a related record (a PO, internal order, requisition, shipment…), promoted from the inbound `linkedOrder` pattern (DESIGN_STANDARDS Unit 7) so outbound/customer-returns stop hand-rolling it three ways (a `data-kind` CSS class, an inline-orange `<A style>`, a bare `<A>`). It IS a router `<A>` (spreads through `href`/`onClick`/router props); what it adds is `kind` → tone only (`po` → `--secondary-main`, `io` → `--primary-main`; omit for a neutral reference) — weight, size and the rest inherit from context — plus the guarantee colour is a **redundant** cue: the kind lives in the label text (`PO-011`/`IO-095`/`#…`), never colour alone (a11y). Registered in the [spec registry](../../../spec/ui-standards/components.md#typography) so a hand-rolled toned related-record `<A>` is a C3 look-alike. Demoed at `#/showcase/typography` (in a `MemoryRouter`, since `<A>` needs a router context).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **UserLabel** — recorded user + email affordance (`ui/elements/typography/UserLabel`)                                                      | Hand-rolled (a `<span>` wrapper + `InfoTooltip` + CSS Modules)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — Nothing to buy: the one treatment for a recorded user's name with their email behind the shared info marker — the "Entered by" / "Edited by" row every detail side panel shows, promoted from five hand-rolled panel copies (two shapes: bare span + inline style, HStack + Text) so panels stop re-inlining it. Renders the name (a dash when none) and, only when an email is known, an `InfoTooltip` whose accessible name is the caller's `label` (the field it annotates); font/weight inherit from context like any value text. Registered in the [spec registry](../../../spec/ui-standards/components.md#typography); recipe in [SIDE_PANEL.md](./SIDE_PANEL.md). Demoed at `#/showcase/typography`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **LabelledValue** — read-only field (`ui/elements/typography/LabelledValue`)                                                               | Hand-rolled (plain `<div>` + a label `<span>` + a `<Text>` value + CSS Modules + tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — Nothing to buy: a stacked label-above-value pair for read-only detail data (a card's secondary fields — ui-standards § tables card layout; also detail/side panels). Owns its own label/value typography keyed off `data-variant` (the value stays a `<Text variant="body">`; colours set here — Text never carries colour). A `variant` picks the label look: **`card`** (default — small + muted `--text-secondary` label; the read-only field in CardView, side panels, and the settings/help version rows) or **`field`** (the label matches an editable input's label exactly — `--text-sm`, medium, `--text-body`, and the input's label→value gap — so a read-only value sits flush beside editable fields in a form; read-only-vs-editable then reads from the ABSENT input box, not a muted label — see [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md)). A **plain block, not a `<dl>`** (key-value-as-description-list is optional for read-only display; a bare block wraps/reuses more freely), so a row of them wraps intrinsically (CLAUDE.md #7) rather than forcing a fixed grid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **TextField** — text input (`ui/elements/inputs/TextField`)                                                                                | Hand-rolled (plain `<label>` + `<input>` + CSS Modules + tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | — Nothing to buy: native input semantics, `label[for]` + `aria-describedby` wiring for helper/error text, `aria-invalid` + icon-and-text error message (no colour-only meaning), asterisk on the label for required. The company input design spec: token heights (default/small), compact/short/long/full width caps — **opt-in max-widths over a `full` default**, and the reference the other input modules match rule for rule, so a text field, a picker, a select and a date field stacked in one form are the same width with nothing passed; `compact` (10rem) caps only the input box, with label/helper wrapping at the short cap ([`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md) § input widths) — orange focus glow (`--focus-ring`), **red error glow via the new `--focus-ring-error` contract token** (the prototype hard-coded this rgba — tokenised per the no-hard-coded-colours rule). `hideLabel` names the input via `aria-label` instead of rendering a label (identical accessible name, no hidden text node to trip the e2e suites’ strict text locators) when a FieldRow shows it. `startAdornment`/`endAdornment` render short text (currency symbol, unit) inside the field frame, aria-hidden — with an adornment the border box moves to a `:focus-within` wrapper; without one the wrapper is `display: contents`, so plain fields render exactly as before. `endAction` seats an _interactive_ control (a real focusable button — PasswordField's show/hide toggle) in that same frame, the end icon-button affordance Combobox's clear button already uses; like the adornments it moves the border box to the `:focus-within` wrapper. Solid port of the prototype's TextField.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **TextArea** — multi-line text input (`ui/elements/inputs/TextArea`)                                                                       | Hand-rolled (plain `<label>` + `<textarea>` + CSS Modules + tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | — The TextField spec on the native `<textarea>`: same label/helper/error/required/aria wiring, focus + error glows and width caps (defaults `full` — the old OMS TextArea fills its container). Height comes from the `rows` prop (default 4, as OMS) and is fixed — longer content scrolls, `resize: none` (OMS/MUI multiline shows no grip); line-height opens to 1.5 for wrapped text. `size` (`default` \| `small`) sets the 13px text + label, matching the small inputs so a multi-line field lines up beside them in a `HeaderToolbar` cluster. Height still comes from `rows`, but the **block padding is derived from the size's `--input-height*` token** (`calc((token - 1lh) / 2 - 1px)`) rather than a spacing step, so `rows={1}` is exactly a TextField's height and `rows={n}` is n lines on that rhythm — a spacing step put a small one-row box at 37.5px against the 36px input, and that 1.5px reads as a ragged edge in a header cluster. **In BOTH rest and touch modes**: under `pointer: coarse` the derivation switches to the `--input-height*-touch` tokens, because the formula is self-cancelling otherwise (the touch font bump grows `1lh` and the rest-mode padding shrinks by exactly as much, so the box stayed at 36px against its 44px neighbours — see the 2026-08-14 entry in [DESIGN_STANDARDS.md](./DESIGN_STANDARDS.md)). `min-block-size`, which every sibling input uses for its touch height, is deliberately not the mechanism here: it would seat the single line at the top of a taller box instead of centring it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **NumberField** — numeric input (`ui/elements/inputs/NumberField`)                                                                         | Hand-rolled: `type="text"` + `inputmode` **wrapped in TextField** (the shared input chrome) — no library                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — The old OMS NumericTextInput rebuilt (never `type="number"` — it can't do locale separators and is the documented source of the old component's bugs). Parent owns `value: number \| undefined`; the component owns the text. **Commits are eager**: every keystroke that parses to a complete number fires `onChange` with a valid (rounded-to-`decimalLimit`, clamped-to-`min`/`max`) value — so a save triggered from inside the field (Enter, or clicking Save) never reads stale state; clearing commits `undefined` (callers must handle it). The _text_ is never rewritten mid-typing (old OMS clamped the display under your fingers — issue 5990); blur/Enter canonicalises: grouping separators, `decimalMin` padding, clamping. Focused text is the ungrouped "edit form" (no caret-fighting separators, select-all on focus); `noFormatting` for numbers where grouping is noise (years) — still a number, so leading zeros collapse on blur (digit strings that keep them, e.g. item codes/barcodes, are TextField's job); ArrowUp/Down step by `step` (Shift ×`multiplier`); digits end-align (logical — flips in RTL); **no numeric-specific width default** — it fills its container like every other input (it capped itself at `compact` 10rem until 2026-08-13, but the container a number sits in is nearly always already narrow — a table cell sized by its column, a `FormRow` share, a side-panel value track — so the cap only ever under-filled it, and 20 call sites overrode it to `full` against 1 that wanted `compact`), so pass `width="compact"` where the value is short AND the container unbounded. Locale-aware via `src/intl` `getNumberSymbols`/`parseNumber`/`formatNumber` (fr comma-decimal + numpad-dot alias, ar Arabic-Indic digits); mobile keypads via `inputmode` (`text` when negatives allowed — numeric keypads lack a minus key). The state machine is pure (`numberFieldLogic.ts`, vitest suite ported from the old OMS tests). A locale or constraint change always reformats the display from the value (the semantic sync guard applies only to value changes). CurrencyField builds on this.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **CurrencyField** — money input (`ui/elements/inputs/CurrencyField`)                                                                       | Hand-rolled: a thin wrapper **over NumberField** (which wraps TextField) — no library                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — Money entry with the whole NumberField contract inherited (eager valid commits, blur canonicalisation, paste repair — `$1,234.56` pastes clean). Per-currency behaviour is **derived from `Intl`, not a hand-maintained table** (`getCurrencyInfo` in `src/intl/currency.ts`; the old OMS table drifted — its KMF entry said 2 decimals, ISO 4217 says 0): `decimalLimit`/`decimalMin` default to the currency's minor units (USD 2 with `1 → 1.00` blur padding; JPY/XOF 0 — the decimal point isn't even typeable), both overridable (4 dp unit costs). The symbol is a TextField **adornment** (chrome, never text), placed per locale (`$1,234.56` en-prefix vs `1 234,56 €` fr-suffix, from `formatToParts` order) and flipping logically under RTL; `currencyDisplay` picks `narrowSymbol` (default) / `symbol` / `code` for `$`-ambiguity cases. `currency` prop for explicit/foreign currencies; the default follows the `homeCurrency` signal — **currency is a store property, not a language property** (old OMS read `store.homeCurrencyCode`); the signal is seeded `USD` with a TODO for the real store wiring (schema field exists; see `src/intl/currency.ts`), and the showcase drives it from a mock selector. Old OMS's CurrencyInput was a second parallel implementation over `react-currency-input-field` with drifted styling and a broken eager-commit check (any trailing zero — so typing "20" never committed); wrapping NumberField makes that drift impossible, and `currency.js` (money math) stays out of the bundle — math is a domain concern.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **PasswordField** — masked password input with show/hide toggle (`ui/elements/inputs/PasswordField`)                                       | Hand-rolled: a thin wrapper **over TextField** — no library                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | — A TextField variant, not a new frame: masking is the standard `type="password"` input, and the only special piece is the show/hide toggle rendered in TextField's `endAction` slot (the eye icon-button, the same end-button affordance as Combobox's clear). The toggle flips the input between `password`/`text` (`aria-pressed`, `Eye`/`EyeOff` glyph, keyboard-focusable like Combobox's clear); label, error line, focus ring and aria wiring all come from TextField unchanged. The toggle's `data-testid` defaults to `<data-testid>-visibility` (the shared e2e contract id, e.g. `sync-settings-password-visibility`). Live in the four password fields: login, re-login, initialisation, and Sync settings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **SearchField** — search input (`ui/elements/inputs/SearchField`)                                                                          | Hand-rolled (native `<input type="search">` + a leading `SearchIcon` and a trailing clear `<button>`, all inside one bordered frame)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | — Its own element, not a TextField variant: the leading icon + clear affordance are structural. A `type="search"` input owes no a11y contract worth buying — the label is visually hidden (the icon is the visual cue) but present for assistive tech, and the clear is a real button. Framed with the same height/border/radius/focus tokens as TextField; the UA's native search clear is suppressed in favour of our own. The current app's SearchBar — drives client-side filtering of a list/detail table (live in the stocktake detail toolbar).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **RadioGroup** — single-choice radio group (`ui/elements/inputs/RadioGroup`)                                                               | Hand-rolled over the **native `<input type=radio>`** (`<fieldset>`/`<legend>` group, shared `name`, hand-drawn control via `appearance: none`) — **no library**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | — A form input, grouped with the inputs (not the selectors — unlike Select/Combobox it needs no `role=listbox` popup). The "own the simple" case: a shared `name` gives the browser single-select grouping, roving arrow-key focus and the radiogroup/radio ARIA for free. We draw the control ourselves (`appearance: none`: a `--primary-main` rim + radial-gradient dot with a **transparent** gap — `accent-color` painted that gap in a computed contrast white in both schemes, a white ring on dark backgrounds) and lay the label — with an optional muted `description` — beside it. Per-option `disabled`, `orientation`, and `indentRem` (line a sub-group up under a sibling control's text). **`appearance="card"`** (#837) draws each option as its own bordered box — whole box clickable, label bold with the `description` running INLINE after it, the chosen one taking a `--secondary-main` rim and a 6% wash of the same colour via `:has(.input:checked)`, so no JS mirrors the checked state. For a short set of mutually exclusive MODES that reshape the screen, not for an ordinary yes/no. `hideLabel` for use in a FieldRow. Live in the create-stocktake modal (the three mode cards + the plain include-all choice).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **FieldRow** — inline label + control row (`ui/elements/inputs/FieldRow`)                                                                  | Hand-rolled layout only (grid: `minmax(6rem, auto)` label column + `minmax(0, 1fr)` control)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | — A compact form row (the current app's dense dialog/panel layout): a bold label on the inline-start, the wrapped control filling the inline-end. Pure layout — the control keeps its own look but **hides its own label** (`hideLabel`) so the row is the single visible label, still announced to assistive tech. RTL-safe (logical properties). Pairs with InsetPanel; live in the create-stocktake filtered form.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **BareCheckbox** — the one drawn checkbox control (`ui/elements/inputs/BareCheckbox`)                                                      | Hand-rolled over the **native `<input type=checkbox>`** (invisible full-size overlay — the real click target and state owner) + a styled box + `CheckIcon`/`MinusIcon` glyphs — **no library**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — Extracted from Checkbox (2026-07-24, tables Phase 2 unit 8) so every checkbox in the app renders the SAME box by construction: Checkbox composes it for forms; the DataTable renders it bare (aria-label) in selection cells + card view. Adds the tri-state the form control never had: an `indeterminate` prop sets the DOM property (ref'd effect) and shows the MinusIcon dash. `error` reddens the box. The input hides via `opacity: 0` at full box size (not sr-only clipping) — keeps a bounding box so e2e `.check()` on the test-id'd input works and clicks land on the box. Root is block-level flex on purpose: inline-level roots would hand table cells a line-height strut taller than the 18px box and break the compact 40px row floor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Checkbox** — labelled checkbox (`ui/elements/inputs/Checkbox`)                                                                           | Label + error chrome composing **BareCheckbox** (which owns the native input + drawn box), all inside a `<label>` — **no library**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — The "own the simple" case: native semantics + keyboard (Space) + `disabled` come free; a label click toggles it (the `<label>` wrap). The box + check glyph read the input's `:checked` / `:focus-visible` off adjacent-sibling selectors (the check IS the state, never colour alone); `error` shows an icon + message and reddens the box (`aria-invalid` + `aria-describedby`). Label/message typography matches TextField (`--text-sm`/`--weight-medium`, `--text-xs` error), `--focus-ring`/`--focus-ring-error` glow. `onChange(checked)`. **`focusTarget`** binds a [`createFocusTarget()`](../utils/createFocusTarget.ts) handle to the native input the drawn box decorates ([`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **ToggleSwitch** — labelled on/off toggle (`ui/elements/inputs/ToggleSwitch`; named to avoid SolidJS's `<Switch>` control-flow component)  | Hand-rolled over the **native `<input type=checkbox role=switch>`** (visually hidden) + a styled track + sliding thumb, inside a `<label>` — **no library**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | — A checkbox IS a switch to assistive tech once `role="switch"` + the on/off state are set, so keyboard (Space) + `disabled` come free with no library. The custom track fills with the brand tone and the thumb slides to the inline-end (logical `translateX`, RTL-mirrored) when on — the position carries the state (never colour alone). `:focus-visible` ring off the hidden input; `prefers-reduced-motion` drops the slide. `onChange(checked)`. For a binary setting where a slider reads more naturally than a tick box.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **DateField / DateRangeField / DateTimeField / TimeField** — date/time inputs (`ui/elements/inputs/`)                                      | Our markup + CSS over **corvu**'s headless `@corvu/calendar` (`DatePickerPanel`, single + range modes, day/month/year grid views) hosted in our **Popover**, plus **Kobalte's `TimeField`** for the segmented time. A **typed text input** parses/formats the date (`dateTimeConvert.ts`). Shared `FieldShell` (label/helper/error, mirrors TextField) + `DateTimeFields.module.css` (control frame) + pure `dateTimeConvert.ts` helpers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **corvu `@corvu/calendar`** (~4 KB gzip, plain JS `Date`, Gregorian only) — the WAI-ARIA calendar-grid keyboard/focus contract (arrows / Home-End / PageUp-Down) is the dangerous-to-hand-roll part worth buying; we render every cell, so it looks like the app and — unlike native `<input type=date>` — renders **identically in every browser** (Firefox exposes no CSS hooks for native date controls). Navigation has **day / month / year grid views** driving corvu's `setMonth` (no full-height native `<select>`). The date is **typed or picked**: a text input parses/formats per a `format` token string (`formatIsoDate`/`parseDateInput` — `dd/MM/yyyy`, `dd MMM yyyy`, …; parse is order-aware + lenient, invalid reverts). **`@kobalte/core/time-field`** (no new dep — Kobalte is already in; value is a plain `{hour,minute}`, no `@internationalized/date`) supplies the **segmented** spin-button time — type or arrow-step, can't hold an invalid value; `hourCycle` picks 12-/24-hour. **Value contracts** ([`spec` inputs § dates & times](../../../spec/ui-standards/inputs.md#dates--times)): `DateField` = ISO `YYYY-MM-DD` (schema `Date`); `DateRangeField` = `{start,end}` ISO pair (pick-only — with a footer under the calendar stating both sides, each with its own clear plus a "Clear dates" for the pair, since the start-then-end pick can't otherwise express a ONE-SIDED range, and both wire conversions already drop a null side); `TimeField` = plain `HH:mm` (no `Time` scalar); `DateTimeField` = typed/picked date + segmented time in **one input frame**, stored as a **UTC instant** (`DateTime<Utc>`) but edited as **local wall-clock**, converting at the **device timezone** (plain JS, no `date-fns`). All pass through / clear to `null`; `min`/`max` disable out-of-range days via corvu's `disabled` predicate. Both the date buffer and time buffer are local signals resynced from the value only on external change (so typing isn't clobbered and time survives being set before a date — kdd/solid-reactivity-pitfalls); `FieldShell` renders its control **once** (in the body, not JSX) to avoid remounting on `helperText` change, and carries TextField's **`labelInfo`** slot (an `InfoTooltip` beside the label, outside the `<label for>` so its name doesn't leak into the control's) — the place for a standing explanation, e.g. why a date is disabled, that would drag a dense header row taller as `helperText`. The Popover gained a render-prop `close()` (dismiss on day-pick, not month-nav) and `triggerProps` (label/aria wiring). Non-Gregorian calendars would need a picker on `@internationalized/date` (revisit). **`focusTarget`** binds a [`createFocusTarget()`](../utils/createFocusTarget.ts) handle to each field's first focusable — `DateRangeField`'s popover trigger, `DateTimeField`'s date input — neither reachable by a plain ref; `DateTimeField` also gained `testId`, stamped on that same date input rather than a wrapper ([`kdd/focus-targets`](../../../kdd/focus-targets/draft-kdd.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **InsetPanel** — recessed grouping panel (`ui/layout/InsetPanel`)                                                                          | Hand-rolled, pure CSS + tokens (tinted rounded container: `--bg-group-light`, `--radius-md`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | — Groups a set of related controls with an optional muted `hint` line at the top — the app's "extra options" area inside a dialog. No interaction or a11y contract to buy, just a container. A **layout/grouping** piece (moved `feedback/` → `inputs/` 2026-07-10, then → `layout/` on Carl's review — it communicates nothing; it pairs with FieldRow). Demoed at `#/showcase/inset-panel` (Layout Elements). Live in the create-stocktake modal (the type/include-all/filter blocks).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **WidgetCard** — clickable dashboard tile (`ui/elements/display/WidgetCard`)                                                               | Hand-rolled: a polymorphic `<a>` (router `href`) **or** `<button>` (`onClick`) via `Dynamic`, one bordered raised tile with an optional icon + title + subtitle                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | — No library: the **whole card is a single native interactive element** (link or button), so role + keyboard/Enter/Space + focus come free and there's no nested-interactive trap; the title is the accessible name, the icon `aria-hidden`. `href` renders an `<a>` (@solidjs/router intercepts local anchor clicks, so it routes); `onClick` renders a `<button>`. Hover lifts it (shadow + translate), `:focus-visible` shows the ring — all tokens, `prefers-reduced-motion` honoured. The dashboard-grid tile the reports vertical needs; icon prop resolved once via `children()` (kdd/solid-reactivity-pitfalls §3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **QrCode** — QR symbol as SVG (`ui/elements/display/QrCode`)                                                                               | Hand-rolled SVG over a **vendored, dependency-free encoder** (`ui/elements/display/qr/qrEncoder.ts`) — dark modules drawn as one `<path>` of rects on a light `<rect>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | — A Solid analogue of the reference app's `react-qr-code` (React-only, can't run here). We **vendor the encoder** (a small pure algorithm, ISO/IEC 18004; trimmed Nayuki MIT port) rather than take a QR dependency — a per-PR bundle event that "needs to be worth it" (kdd/bundling) and the "own the simple" case (CLAUDE.md anti-defaults). Encoding memoised on `value`/`ecc`; one `<path>` = one draw call + tiny DOM; `shape-rendering="crispEdges"` scales to any `size`; colours from the theme-invariant `--qr-foreground`/`--qr-background` tokens (a QR stays dark-on-light in both themes for scanning — outside the theme contract). The inline symbol is **always click-to-expand** — a button that pops an enlarged copy via the shared `Popover` (composed, not re-bought). A QR is only ever used to scan/pair (the reference app's sole usage, Admin/ServerInfo), so expand is the element's behaviour, not an opt-in prop — no static consumer justifies the API (reintroduce if a print/report context ever needs one). The matrix is encoded once and shared by the trigger and enlarged copies; pass `title` for the enlarged symbol's accessible name (`role="img"`). Correctness independently verified by round-tripping the output through jsQR (v1–v8, all four ECC levels, UTF-8) and frozen as a golden in `qrEncoder.test.ts`. Used by Settings' server-info block (issue #500).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **DocumentFrame** — sandboxed document iframe (`ui/elements/display/DocumentFrame`)                                                        | Hand-rolled: a `<div>` wrapper + sandboxed `<iframe>` (`src` or `srcdoc`) + a Spinner loading overlay                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — The wrapper for server-rendered HTML report output. Required `title` (the iframe's accessible name); `sandbox="allow-same-origin"` by default (the document may pull same-origin assets like images but runs **no scripts** — no `allow-scripts`), choose deliberately. The two are **alternatives, never a pair**: a frame granted both can reach the parent and strip its own `sandbox`, so a document that must run scripts (the reports view's, [S2](../../../spec/reports/ui-surface.md#s2--report-detail)) passes `sandbox="allow-scripts"` **instead**, landing on an opaque origin with no cookie, storage, or app access. Fills its container; a centred `Spinner` (`center`) overlays until the iframe's `load` event, and the loading flag **resets on source change** (`on([src, srcdoc], …, { defer:true })`) so a re-navigated frame shows the spinner again. Nothing to buy — the platform iframe is the sandbox.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **CardGrid** — responsive card grid (`ui/layout/CardGrid`)                                                                                 | Hand-rolled, pure CSS + tokens (one `<div>`; intrinsic `grid-template-columns: repeat(auto-fit, minmax(min(--card-grid-min, 100%), 1fr))`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | — Nothing to buy: a generic content-layout primitive (ui-standards § Layout) that fits as many equal columns of at least `minColumnWidth` (default 20rem) as fit and wraps the rest, with **no breakpoint maths** (principle #7, intrinsic-first). Owns its CSS so pages/sections compose without styling (principle #10). Mirrors the current app's generic grid container; first consumer is the dashboard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **ContentContainer** — content measure (`ui/layout/ContentContainer`)                                                                      | Hand-rolled, pure CSS + tokens (one `<div>`; `max-inline-size` from `--measure-form`/`-prose`/`-wide` + `margin-inline: auto`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — Nothing to buy: the centred **reading-column measure** a section drops inside the Page body so a form or prose stops widening and centres on a wide monitor. Owns **only** the cap + centring — **not padding** (the Page body owns edge padding, so measure is a content opt-in, not frame geometry: tables stay full-bleed via `fillBody`). `size` picks the cap. See [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **FormColumns + FormColumn** — form section columns (`ui/layout/Form`)                                                                     | Hand-rolled, pure CSS + tokens (FormColumns = flex-wrap row owning the gap; FormColumn = a `flex: 1 1 var(--form-column-min)` vertical stack of sections)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | — Nothing to buy: the row of section stacks in a multi-column form. The author places each `FormSection` in a column **explicitly** (chosen over an auto-fit grid — [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md)), so grouping is click-traceable; the columns share width and **wrap to a single stack** when squeezed below `minWidth` (default 22rem), intrinsically, no breakpoint (principle #7). Single-column reading order is column-1 sections, then column-2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **FormSection** — titled field group (`ui/layout/Form/FormSection`)                                                                        | Hand-rolled, pure CSS + tokens (`<section>` with a `Dynamic` `h2`/`h3` heading — neutral, ruled — over a vertical field stack)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | — Nothing to buy: a form’s "Batch & Dates" / "Storage & Pack" group. Fields sit **directly** inside (one per line at full width); the two-up ones are wrapped in a `FormRow`. The heading is **neutral** (`--text-body`, hairline rule) — distinct from the action-blue dashboard `SectionTitle` (a different shape). Heading **rank** and **treatment** are separate props, so the outline stays right independent of size (WCAG 2.2 — same idea as `Text`’s `level`): `headingLevel` (`h2` default / `h3` when the surrounding surface already owns the h2) and `heading` (`group` ruled / `subgroup` quieter). The treatment defaults from the rank (h2 → `group`, h3 → `subgroup`), so callers name both only where they diverge — `headingLevel="h3" heading="group"` for a top-level group of a dialog or card whose own title holds the h2 (the prescription line editor). They were one prop until the line editor showed the cost: a group heading forced to h3 for the outline rendered quieter than the field labels beneath it. See [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **FormRow** — two-up field row (`ui/layout/Form/FormRow`)                                                                                  | Hand-rolled, pure CSS + tokens (one `<div>`; `flex-wrap` row, children `flex: 1 1 var(--form-row-min)` + `min-inline-size: 0`, `align-items: start`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | — Nothing to buy: puts two (or N) fields side by side on the rows that pair (Expiry / Manufacture, Cost / Sell). **Opt-in per row** — a single field needs no FormRow. **Equal shares are the default, not the only shape**: each child shares the width and the row **stacks** when it can’t hold them at `minItemWidth` (default 10rem), intrinsically (principle #7) — a child whose data wants a bigger or smaller share wraps in a `FormRowItem` (next row) to declare its own weight and min. That wrapper sizes **itself**, inline, so this file’s own item rule is **unchanged** by weighting (`.row > * { flex: 1 1 var(--form-row-min); min-inline-size: 0 }`) and an unwrapped sibling — a bare field, HeaderToolbar’s trailing compact `Alert` at its own `flex: 0 1 auto` — keeps exactly the sizing it always had. Weighting added one line here: `--form-row-item-min: var(--form-row-min, 10rem)` on `.row`, purely the floor a `FormRowItem` inherits when it names no `minWidth` of its own — nothing else reads it. `align-items: start` keeps a short control (a checkbox) top-aligned with a taller sibling. Never styles the controls. See [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **FormRowItem** — one weighted row slot (`ui/layout/Form/FormRowItem`)                                                                     | Hand-rolled, **no CSS of its own** — one `<div>` that sizes itself with inline declarations — `flex: <weight> 1 0`, `min-inline-size: min(<minWidth>, 100%)` and an optional `max-inline-size: <maxWidth>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | — Nothing to buy: **opt-in per item**, exactly as `FormRow` is opt-in per row — a field dropped straight into the row keeps the equal share, and wrapping it here says _this field’s data needs more (or less) room than its neighbours’ does_. `weight` (default 1, so a bare field and `weight={1}` size identically) is the item’s **`fr` share of the row**: the inline `flex-basis` is `0`, so the row’s WHOLE width distributes in proportion to the weights — not just the space left after every item has taken a basis — which is what makes it the [design standard](../../../spec/ui-standards/layout.md)’s `fr` weight; `weight={0}` pins the item to its floor and hands every spare pixel to its siblings (a formatted date can never use more width, so it is pinned rather than claiming an equal column). The floor is `minWidth`, carried by `min-inline-size` — where flexbox’s min-violation pass clamps it exactly as the `min` of grid’s `minmax(min, Nfr)` does — capped `min(…, 100%)` so a single item on a line narrower than its floor still shrinks instead of overflowing; it overrides the row’s `minItemWidth` for that slot alone and decides **who gives up width** as the row narrows — a field that reaches its floor stops shrinking and its siblings absorb the rest, so a real floor is what protects a name field, while which fields drop to the next line is document order (the trailing ones); that plus the row’s existing `flex-wrap` is the whole responsive story, no breakpoints (principle #7). **The floors are a budget**: their sum is the WRAP point, so a cluster’s floors must sum to no more than the unweighted row’s would (prescriptions: 11 + 10 + 9 + 10 + 9.5 + 9.5 = 59rem against 6 × 10rem) — an earlier 13/12/9/11rem set came to 63rem and measured out wrapping the cluster onto a second line at a ~1366px window where it fits on one line today, growing the header by a row. **Not a grid and not a column template**: the field count is dynamic (`CustomFieldsToolbar` renders 0..N prominent fields), so no authored `repeat(N, 1fr)`-style template stays correct — the weight is the `fr` and `min-inline-size` the `minmax()` floor. Because the item sizes itself rather than the row sizing it, every unwrapped sibling is provably untouched — which is also why you must **never** wrap HeaderToolbar’s trailing compact `Alert` (or the `ToggleSwitch` doing that job) in one: `HeaderToolbar.module.css`’s `.fields [data-compact] { align-self: flex-end }` would then resolve against the wrapper `<div>` and the chip would silently lose its bottom-hug. Pure layout: it never styles the control inside — the control fills the slot the row hands it because that is every input's default, not because the slot made it. In use: the prescription header (`src/sections/prescriptions/detail/PrescriptionToolbar.tsx` — Patient `1.9`/11rem, Date `0`/9rem, Clinician `1.55` and Program `1.2` on the row’s own 10rem; measured on the six-field reference store, the patient field’s value room goes 45 → 87px on a 1060px row and 97 → 220px on a 1372px one, where the full 26-character “MOHAMED, DJIBRIL ABDULLAHI” fits for the first time — at a byte-identical header height and an unchanged wrap point) and `CustomFieldsToolbar`’s `layout="field"`, where a prominent `option`/`number`/`date` field takes `0.9`/9.5rem and `text`/`boolean`/`unsupported` keep the equal share. Demoed at `#/showcase/header` (the weighted-cluster card). See [`kdd/form-layout`](../../../kdd/form-layout/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Stack** — vertical block stack (`ui/layout/Stack`)                                                                                       | Hand-rolled, pure CSS + tokens (flex column; `gap` presets `sm`/`md`/`lg` → `--space-2/4/6` via `data-gap`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | — Nothing to buy: a vertical run of sibling blocks with a consistent gap (a detail form's identity header / alert / form columns; a modal's lookup-row pair). **Vertical only, by design**: horizontal grouping usually has a more semantic owner that also encodes its wrap behaviour (`FormRow`, `Columns`, `FormColumns`, `HeaderButtons`, `ContentFooterActions`, `CardGrid`) — or the generic **`HStack`** when none of those fit. Gap is a **preset, not a length**, so block rhythm stays consistent app-wide.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **HStack** — horizontal block stack (`ui/layout/Stack/HStack`)                                                                             | Hand-rolled, pure CSS + tokens (flex row; `gap` presets `sm`/`md`/`lg` → `--space-2/4/6`, plus `align`/`justify`/`wrap` via `data-*`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | — Nothing to buy: the generic horizontal counterpart to `Stack` — a "value + affordance" row or button cluster that **isn't** one of the semantic horizontal owners (`FormRow`, `HeaderButtons`, `ContentFooterActions`, `CardGrid` — prefer those). Block-level + full-width like `Stack` so `justify` (default `start`) has slack to distribute; `align` (cross-axis) defaults to `center`; `wrap` for clusters that must survive narrow widths. Gap is a **preset, not a length**. Registered in the [spec registry](../../../spec/ui-standards/components.md#layout) so a hand-rolled flex row is a C3 look-alike. Demoed at `#/showcase/page-layout`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **IdentityHeader** — record name + subtitle header (`ui/layout/IdentityHeader`)                                                            | Hand-rolled (semantic `<header>` + two `Text`s + CSS Modules + tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | — Nothing to buy: the record-identity header atop a sectioned edit form (spec/ui-standards detail-views) — the record's name as the region heading with an optional muted subtitle of secondary identity facts. Heading rank **fixed at h2** (a region heading under the page's breadcrumb h1) — no rank knob until a second context needs one, so a caller can't drag in a different look by picking a rank. Owns the subtitle's `--text-secondary` colour (Text never carries colour). `title`/`subtitle` are JSX so the name can render as a link (the catalogue-record seam).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **DashboardCard** — dashboard widget card (`ui/elements/dashboard/DashboardCard`)                                                          | Hand-rolled, pure CSS + tokens (flex-column `<section>` with an `<h2>` title — optional leading tinted icon chip, aria-hidden — a panels body, and an optional footer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | — Nothing to buy: an equal-height titled card (`--surface-raised`, `--radius-lg`, `--shadow-2`) holding StatsPanels with an optional footer action pinned bottom-inline-end. Presentational — the dashboard section owns the data, gating and footer. Mirrors the current app's `Widget`; named `DashboardCard` (not `Widget`) to stay distinct from the unrelated clickable `WidgetCard` in `ui/elements/display`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **DetailContainer / DetailSection / DetailRow / RecordNameHeader / DetailCard** — the read-only detail-form scaffold (`ui/layout/Detail/`) | Hand-rolled, pure CSS + tokens throughout — `DetailContainer` (centred width-cap), `DetailSection` (label/value grid of `DetailRow`s, optional heading), `DetailRow` (label + disabled control / disabled checkbox / a `control` slot), `RecordNameHeader` (centred emphasised name + optional store icon), `DetailCard` (a titled card, `--surface-raised`/`--radius-lg`/`--shadow-2` — same card family as `DashboardCard` — with a header-end `actions` slot over an arbitrary body)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | — Nothing to buy: the scaffold every read-only detail page builds on (landed with the `names` vertical; `src/sections/names` is the live reference). `DetailCard` is the one shape the rest of the family didn't cover — several records of the same kind stacked as cards (e.g. the items Variants tab: one card per variant, Edit/Delete `IconButton`s in the header, a `DetailSection` + tables in the body) rather than one whole page being the detail view. Demoed at `#/showcase/detail-views`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **StatsPanel** — titled stat group (`ui/elements/dashboard/StatsPanel`)                                                                    | Hand-rolled, pure CSS + tokens (`<section>` inner card + `SectionTitle`, `Switch` over loading/error/ready)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | — Nothing to buy: a group of Statistics as an **inner elevation card** (`--surface-raised`, `--radius-lg`, `--shadow-1`) with a `SectionTitle` heading (optionally linking, via the router `<A>`, to the unfiltered list). Stats stack as a **vertical list** (never multi-across). **Owns its own loading / error state** driven by its count query: `loading` and `error` (a Forbidden family the user can't read) each replace the stats with a muted text line (`loadingMessage` / `errorMessage`; `aria-busy` while loading) — the current app's understated approach, and the repo standard for content-loading (a short text, not a skeleton/spinner); `ready` shows the stats — siblings unaffected. Mirrors the app's `StatsPanel`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **SectionTitle** — iconed action-colour heading (`ui/elements/dashboard/SectionTitle`)                                                     | Hand-rolled, pure CSS + tokens (`<h3>` with an optional leading icon + optional router `<A>`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | — Nothing to buy: the small `--secondary-main` (action-tone) panel heading with a leading box icon (icon inherits `currentColor`, so it matches the title; kept outside the link so it isn't part of the accessible name). Extracted from StatsPanel so the "iconed action-colour title" is one shape. Uses `--secondary-main` — the app's action colour and the library's action-text colour (Button/Dialog/DataTable).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Statistic** — labelled metric, optionally a link (`ui/elements/dashboard/Statistic`)                                                     | Hand-rolled, pure CSS + a semantic router `<A>` (a plain `<div>` when there is no link), wrapped in a plain `<div>` row that also carries the **`InfoTooltip`**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | — Nothing to buy: with an `href` the `<a>` gives a real link role + accessible name for free (value + label). A large value **beside** its label in a right-aligned column (one row of the panel's vertical list), the whole row linking into the list it counts. **`href` is optional**: a metric with no drill-down omits it and renders as plain text — no link role, no tab stop, and the hover/focus affordances are scoped to `a.main` so a non-interactive row never hints at a click. (A stat MUST NOT self-link to the page it is on: that ships an element announced as a link that leads nowhere — accessibility § assistive-tech parity.) `alert` shows a red **"needs attention" `StatusChip`** beneath (`alertLabel`) — the value stays normal; meaning is carried by the chip's text, never colour alone (accessibility § colour independence); `info` hangs an **`InfoTooltip`** beside the label — hover / focus / tap, named after the stat ("More information about {label}"), so the explanation reaches keyboard, touch and screen-reader users. Its trigger is a `<button>`, which may not nest inside an `<a>`, so **the stat is a wrapper** (`<div>`, and the element carrying `testId`) holding the link and the marker as siblings — same shape with or without `href`. It was a native `title` on an `aria-hidden` span until [#775](https://github.com/msupply-foundation/open-msupply-frontend/issues/775), reachable by neither keyboard nor assistive tech; it keeps the app's brand tone via `InfoTooltip`'s `tone="primary"`. Takes an already-locale-formatted value string — the section formats. Mirrors the app's `Statistic`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **PluginRegionOutlet** — plugin contribution mount point (`ui/elements/plugins/PluginRegionOutlet`)                                        | Hand-rolled: a `<For>` over contributions, each in its own solid-js `ErrorBoundary`; pure CSS + tokens for the fallback line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | — Nothing to buy: the mount point where plugin contributions render inside a dashboard container (rendered as the last child of the card grid, a widget's panel column, and a panel's stat list — ui-standards § Dashboard). Renders exactly what it's given in the order it's given — merging/sorting is the dashboard's `mergeRegion`, not the outlet's. **No wrapper element**: each contribution becomes a direct child of the surrounding container and adopts its geometry, so an empty region renders nothing and reserves no space. Each contribution gets its own error boundary — one throwing contribution shows a neutral muted text line (`errorFallback`, already translated) in its own slot only (dashboard AC-D6; plugins rules § error isolation). Contributions arrive as data (`{ id, Component }`) — the sanctioned exception to explicit composition, same as the DataTable's config-driven columns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **PluginSlotOutlet** — props-carrying plugin mount point (`ui/elements/plugins/PluginSlotOutlet`)                                          | Hand-rolled: a `<For>` over contributions, each constructed inside its own solid-js `ErrorBoundary`; no CSS at all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — The sibling of PluginRegionOutlet for a slot whose contributions receive **props** — the internal-order line editor's info panel (plugins sdk-contract § the info-panel slot). Same rules: renders what it is given in the order given, **no wrapper element** (an empty set renders nothing and reserves no space), one error boundary per contribution, contributions as data. What it adds is the guarantee the line editor's walk needs: the slot props arrive as an **accessor**, and the outlet binds each key to a getter over it, so a new record reaches a LIVE contribution — no remount, no lost contribution state, no detached `<dialog>` (AC-PLUG-N2, kdd/solid-reactivity-pitfalls). Its key set is resolved once, untracked: enumerating a reactive props source inside a tracked scope is what makes the obvious `<Dynamic {...mergeProps(() => slotProps())} />` remount on every change (its colocated test pins this). The fallback for a throwing contribution is bare translated text rather than a styled element, matching the contributed table cell's — the region imposes no box of its own.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **AppLogo** (`ui/branding/AppLogo`)                                                                                                        | Hand-rolled: a `<Show>` over the custom-logo signal — a site's sanitised SVG inlined into a sizing `<span>`, else `MSupplyGuyLogo`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | — The single logo call site for the whole app (docked + overlay drawer, login hero, initialisation hero), so a site's branding is set in one place; the caller's class sizes the box, so the custom and stock logos are interchangeable. A site's SVG is sanitised once on arrival via `DOMParser` (`applyBranding.sanitiseSvg` — drops `<script>`, `on*` handlers, `<foreignObject>` and external `href`s, keeps same-document `#refs`), which the reference app does not do; it matters because branding applies **pre-authentication** on the login hero. Anything unparseable falls back to the stock logo rather than rendering a blank hero. See [`kdd/custom-themes`](../../../kdd/custom-themes/draft-kdd.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Icons** (`ui/icons`)                                                                                                                     | Hand-rolled plain SVG (chevron, check, close, search; action set — plus/download/save/print/trash/alert-triangle/alert-circle/x-circle/copy/minus-circle/message-square/info/info-outline/arrow-right/lock/sidebar; nav set — stock/truck/customers/thermometer/file/clock/reports/list/sliders/settings/radio/help/map-pin/calendar; report-category set — bar/suppliers (truck + file reused); footer set — home/edit/user/translate/central; filled hamburger; MSupplyGuy logo)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | — Copied wholesale from the current app (via the prototype's port): same Fill/Stroke `currentColor`/`1em` convention; only the icons actually in use, added as needed. The hamburger is the app's **filled** MUI Menu bars (not a stroked outline). The MSupplyGuy logo is the full original artwork (body + arm + register box), except that the three white fills over the figure — cap band, collar, leg swoosh — are **cut out** of the silhouette (subpaths under `fill-rule="evenodd"`) rather than painted over it, so they take the surface's own colour instead of being bright blobs on a dark one. The figure keeps its own orange→red brand gradient, but the **jar is `currentColor`** — the app's text colour — so it inverts with the theme instead of sinking into a dark surface, and its cross is knocked out of it (a hole shows the surface, which contrasts with the text colour by definition). The original's keylines are dropped: as user-space strokes they went sub-pixel at drawer size and fringed every edge (a stroke that IS wanted here needs `vector-effect="non-scaling-stroke"`). Note `currentColor` means a wrapping `<button>` must set `color: inherit` — the drawer's home button does. **Directional icons** (arrow-right) carry `data-flip-rtl`; a global `[dir='rtl']` rule in `index.css` mirrors them — the plain-CSS analogue of the app's `RtlFlipIcon` wrapper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **ShortcutBadge** — key-combination hint badge (`ui/elements/keyboard/ShortcutBadge`)                                                      | Hand-rolled, pure CSS + tokens (absolutely positioned, monospace, `--gray-dark`/`--bg-white`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | — Nothing to buy. Reveals a control's key combination while Alt or Ctrl is held ([keyboard S2](../../../spec/ui-standards/components.md#menus-popovers--modals), KB-H1). Takes the declared `Shortcut` **value**, not a string, so the badge and the control's `aria-keyshortcuts` render from one source and cannot drift (AC-KB15). Absolutely positioned so appearing on Alt-down never shifts a toolbar; sits just inside the carrier's corner because a carrier may clip overflow (`Button` does, for its ripple). A carrier that the default corner does not suit passes a placement `class` (scoped under its own class so it wins the cascade regardless of sheet order) — `IconButton` drops the badge below its box. Reads the `modifierHeld()` module signal, so a screen with no shortcut-carrying controls renders nothing. `aria-hidden` — the binding is already exposed via `aria-keyshortcuts`. Demoed at `#/showcase/keyboard`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **CommandPaletteView** — search-and-run action overlay (`ui/elements/keyboard/CommandPaletteView`)                                         | Composes `Dialog` (native `<dialog>` + `showModal()`) + `TextField` + a hand-rolled `role=listbox`; **no `cmdk`-style library**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | — Nothing worth buying: a text field, a filtered list and an arrow-key highlight, none of which carries an a11y contract (`kdd/own-simple-buy-hard`). Composing `Dialog` is what makes it layer over an already-open dialog (top layer, KB-P1/AC-KB8) and gives Escape-to-dismiss through the UA close request (KB-X1) for free. It takes that Dialog `chromeless`, so there is no card behind it: the search field and the option list float over the dimmed page on their own surfaces, the list using the same vocabulary as a Select/Combobox popup (raised surface, value border, `--shadow-4`, `--bg-group-main` highlight) — they read as one panel by adjacency, not by a shared background. Presentational — the app-side host `src/keyboard/CommandPalette.tsx` supplies the rows from the action registry. Highlight is exposed via `aria-activedescendant` with focus kept in the search field; arrows **wrap** (KB-N3's shape; KB-N1's clamped list-table row focus is a different rule, and not built — see [`kdd/keyboard-layer`](../../../kdd/keyboard-layer/draft-kdd.md)). Demoed at `#/showcase/keyboard`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## Cross-cutting (not a single element)

- **Design tokens** — `src/ui/styles/tokens.css`, ported from the RnD prototype (itself a faithful port of the current app's `theme.ts`), restructured with an enforced **theme contract** region (see [`kdd/ui-styling`](../../../kdd/ui-styling/draft-kdd.md)). Includes the dark theme with the inverted-surface elevation model (`--surface-raised`, near-black dark shadows), and the six `--status-*` shipment-status colours (StatusChip; demo values pending a designed palette).
- **Root stylesheet** — `src/index.css`: box-sizing reset, the single root font-size knob (rem scaling; phone < 600px drops root to 85%), body defaults from tokens, Inter Variable via `@fontsource-variable/inter`.
- **CSS integrity tooling** — `npm run check`: typed-css-modules `.d.ts` generation → `tsc -b` → stylelint (incl. unknown-`var()` detection against `tokens.css`) → theme-contract script → page-CSS check (`scripts/check-page-css.mjs`: pages own no CSS — see [`kdd/page-composition`](../../../kdd/page-composition/draft-kdd.md)). See [`kdd/ui-styling`](../../../kdd/ui-styling/draft-kdd.md).
- **Responsive primitive** — `hooks/createMediaQuery.ts` (a `matchMedia` → Solid signal) + `styles/breakpoints.ts` (`navOverlay: 1024`, `compact: 600`). The **only** JS that touches responsiveness, and only for "which element renders" decisions (principle #7); everything visual stays in CSS. Ported from the prototype's `useMediaQuery`/`breakpoints`.
- **Favicon** — `public/favicon.svg` is the MSupplyGuy brand mark (same paths/gradient as the `MSupplyGuyLogo` icon, minus its small-size keyline treatment — at 16px the whole figure is a blur either way, so a favicon-sized mark is its own crop-and-simplify job), replacing the Vite/Solid default so the browser tab matches the app.

## Dependencies

- **Runtime:** `solid-js`, `@fontsource-variable/inter` (font asset only), `@kobalte/core` (headless behaviour + ARIA for the selector family, the DropdownMenu buys and the tab bar — provisional, see [`kdd/own-simple-buy-hard`](../../../kdd/own-simple-buy-hard/draft-kdd.md)).
- **Dev-only tooling:** `typed-css-modules`, `stylelint` (+ `stylelint-config-standard`, `stylelint-value-no-unknown-custom-properties`), `concurrently`.

_Keep this table in sync as elements are built or change._
