# Issue #550 — UI Library Gap Audit: Progress Tracker

Tracking doc for [#550](https://github.com/msupply-foundation/open-msupply-frontend/issues/550). Not for commit — working scratchpad across sessions.

**Progress legend:** ⬜ Not started · 🔵 In progress · ✅ Done · ⏭️ Skipped/won't-do · ❓ Needs decision

---

## Workflow (per item — follow this every session)

For each item we tackle, work in three steps and pause for Carl between them:

1. **Evidence** — show 1–2 concrete call sites that the new/shared component will replace (real file + line refs, ideally the actual markup/logic). This anchors what "done" means before writing anything.
2. **Implement** — build the component/extraction and swap the identified sites over to it. Keep JSX explicit per the KDDs; extract logic, not markup, unless the item is explicitly a primitive.
3. **Review** — re-read the diff for correctness, reactivity pitfalls (`kdd/solid-reactivity-pitfalls`), bundle cost, and adoption completeness; run `pnpm check` / relevant tests. Then update this doc's Progress column and Notes.

Tier 4 items are **decisions to surface**, not refactors — for those, step 2 is "write up the question for the team," not "implement."

---

## New-component checklist (do ALL of these every time we add a library component)

Adding the component isn't done until every doc/surface that indexes it is updated. Missing one (we missed `UI_ELEMENTS.md` for `HStack` the first pass) leaves the component un-discoverable and — critically — **un-enforced** for spec-driven rebuilds. When adding a new `src/ui/` component:

- [ ] **`spec/ui-standards/components.md`** — register the **role → component** row in the right section. This is what makes conformance requirement **C3** bite: an unregistered component can be hand-rolled without it counting as a "bespoke look-alike". _Non-negotiable._
- [ ] **`spec/ui-standards/<area>.md`** — if the component introduces or changes a cross-cutting rule, add/point a line (e.g. `layout.md` for layout primitives, `inputs.md`/`controls.md`/`tables.md` for those kinds).
- [ ] **`src/ui/docs/UI_ELEMENTS.md`** — add the ledger row (what it's made of + why); mandated by `src/ui/CLAUDE.md`.
- [ ] **Showcase** — add a real demo (the component itself, no showcase-only chrome) to the relevant showcase page, and register it in the page metadata/TOC.
- [ ] **Sibling docs/comments** — update anything that referenced the gap (e.g. `Stack`'s doc comment + card once `HStack` existed).
- [ ] **KDD** — if there's a real rejected alternative, write/extend a `kdd/` entry.
- [ ] `pnpm check` green.

---

## Tier 1 — Genuinely missing pure primitives (belong in `src/ui/`)

| #   | Item                                                                                             | Priority | Progress | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------ | -------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **`HStack`** (or `direction`/`justify`/`align` on `Stack.tsx`) — retires ~35 inline-layout sites | High     | 🔵       | Built `HStack` (sibling of `Stack`, `ui/layout/Stack/HStack.tsx`): `gap` sm/md/lg, `align` (def center), `justify` (def start), `wrap`; full-width block like Stack. **~10/42 sites swapped** (OutboundSidePanel entered-by + tax rows; **Settings sweep 2026-07-28**: `.toggleRow` ×2, `.actions` ×4 → `justify="end"`, `.scannerActions` → `wrap`, `.addRow` — all custom gaps snapped to presets), `pnpm check` green. Showcase demo added (Page layout page). Registered in `spec/ui-standards/components.md` (Layout) + `layout.md` (enforcement — C3 now covers it). Enforcement guard raised as **#639**. Ledger row added to `src/ui/docs/UI_ELEMENTS.md`. **Deferred:** the ~40-site sweep folds into Carl's later full component-replacement pass (visual inspection across the whole UI); `as="span"` opt-in only if a label-context row forces it |
| 2   | **`DropdownMenu`** (generic action/overflow menu)                                                | Med      | ❓       | **Deferred — needs design decision (#643).** Not a naive gap: the 3 existing menus (`UserMenu`, `LanguageSelector`, `SplitButton`) all use Kobalte `dropdown-menu`, but split into **two intentional popup-surface looks** — inset rounded pills (AppShell chrome) vs edge-to-edge divided rows (SplitButton, deliberately per `ui-standards #btn-split`). Building the primitive first needs a ruling: (a) sanction two variants, or (b) one house menu style. No pending consumer; low urgency. Trigger stays a caller slot regardless. Revisit when a real `⋮`/overflow consumer appears or during a Menus design pass                                                                                                                                                                                                                                     |
| 3   | **`RecordLink`** (kind-toned link to a related record)                                           | Med      | 🔵       | Built `RecordLink` (`ui/elements/typography/RecordLink`): router `<A>`, **tone-only** via `kind` (`po`→secondary, `io`→primary, omit→neutral) — weight/size **inherit from context** (Carl); kind-in-label a11y cue. **4/4 app sites migrated** + `TableShowcase` migrated (wrapped in `MemoryRouter`); old `linkedOrder.module.css` **deleted**. Showcase demo added (typography). Registered in `components.md` + `UI_ELEMENTS.md`. `pnpm check` green. **Only visual verification remains (Carl doing it).**                                                                                                                                                                                                                                                                                                                                               |
| 4   | **`ErrorDetails`** (raw error/JSON behind `<details>` in an Alert)                               | Low      | ⬜       | Built twice (SyncModal, ReportDetailView)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 5   | **Labelled-field 4th cell** — inline read-only value                                             | Low      | ⬜       | Line-editor fakes it with `FieldRow labelWidth="auto"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 6   | **Fixed-narrow input width** (e.g. `width="date"`)                                               | Low      | ⬜       | Folds in Tier 3 `DateField` bypass; `type="date"` in 4 files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 7   | **Linear `InlineProgress`** + centered **`BasicSpinner`** (w/ message)                           | Low      | ⬜       | Reference-parity                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

**Reference-parity, adopt-when-needed (don't pre-build):** `WizardStepper`, `ToggleButtonGroup`, `HierarchicalOptionAutocomplete`, content `Skeletons`, `InputModal`, `ListSearch`.

---

## Tier 2 — Un-extracted logic primitives ("extract logic, keep JSX explicit")

| #   | Item                                                                 | Priority | Progress | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------------------------------------------------------- | -------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8   | **`createListController<State,SortKey>`** — biggest logic dedup      | High     | ⏭️       | **Split out to its own issue #653** (app-layer logic extraction, not UI-library). ~12 verticals, 35–45 lines each; owns sort/filter/page/selection. Load-bearing: must preserve the serialised-key + non-suspending `.latest` reactivity rules                                                                                                                                                                                                                                                                  |
| 9   | **`activityLogFormat` helper + adopt shared `ActivityLogPanel`**     | High     | ⏭️       | **Split out to its own issue #665** (app/domain-layer, not UI-library — shared `ActivityLogPanel` already exists at `src/domain/activityLog/`). Kills 2 verbatim ~160-line twins (StocktakeLogPanel, InboundShipmentLogPanel) + their redundant graphql/generated pairs; point both detail views at the shared panel (`recordId`). No `activityLogFormat` symbol exists — format block already lives once in the panel. Outbound/CR/internal-orders LogTabs are simpler (no Details column) — optional Action B |
| 10  | **Adopt existing `createNextItemWalk`** in stocktake + inbound       | Easy win | ⏭️       | **Split out to its own issue #666** (app-layer logic adoption, not UI-library). Shared unit-tested helper lives in outbound (`nextItemWalk.ts`); stocktake + inbound re-hand-roll ~55 lines each. **Not just dedup** — the hand-rolls carry a latent mid-refetch race (scan reactive `rows()`) + no abort guard that the helper fixes                                                                                                                                                                           |
| 11  | **`FilterDatetimeRange`** control / `datetimeRangeFilterDef({zone})` | Med      | ⏭️       | **Split out to its own issue #663** (app-layer filter-def extraction, not UI-library — the `FilterDateRange` widget is already shared). Not a control: the duplication is the ~11-line `FilterDef` block, repeated **7× across 6 verticals** (outbound/inbound/stocktakes/prescriptions/internal-orders/stock). Only key/label/`type` vary. Helper must keep `label` as an accessor (chip-remount guard)                                                                                                        |

---

## Tier 3 — Adoption gaps (component exists; verticals bypass it)

| #   | Item                                                                          | Progress | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A   | **Extend `ItemSearch`** to serve outbound picker; delete copied CSS           | ✅       | **Done in `7d231690`** (Aneesh T, 2026-07-27). Outbound uses shared `ItemSearch`; copied CSS + capped `itemOptionsResource`/`outboundLineEdit.graphql` deleted (fixes the 1000-item cap). `availableStockOnHand` **not** added — resolved as an accepted delta: row shows "total units in store" like the other verticals (spec S4 moved with it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B   | **Route info-popovers through `InfoTooltip`**                                 | ✅       | **Done.** 5 hand-rolled `<Popover trigger={<InfoIcon/>}>` sites migrated to `InfoTooltip` (CustomerReturnSidePanel, PickedDateField, OutboundSidePanel ×2, DisplaySettingsSection); net −35 lines, `pnpm check` green. Testids preserved. **DisplaySettingsSection was click-open → now hover/focus/tap** (Carl OK'd). Left alone (not info affordances): OutboundLineEditModal `CheckIcon` tick, OutboundSidePanel requisition text-trigger, InternalOrderAncillaryBanner "details" plan. Visual check outstanding                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C   | **`ContentContainer` + `Stack` in Settings** — drop `.measure`/`.sectionBody` | ✅       | **Done.** 7 files: `.measure` → `<ContentContainer size="form" align="start">` (prop is `size`, not `measure`), `.sectionBody` → `<Stack>`, and `.editorBlock` (custom `--space-3`) → `<Stack gap="sm">` (Carl picked sm over md). Sync's `<form>` kept, body wrapped in `<Stack>`; TestScanner nests `ContentContainer`>`Stack`. All 3 classes deleted from `Settings.module.css`; `styles` import dropped from Support/Config. Zero visual change (exact token match). Then **swept the horizontal clusters to `HStack`** (see #1): `.toggleRow` ×2, `.actions` ×4 (`justify="end"`), `.scannerActions` (`wrap`), `.addRow` — 4 more classes deleted, `styles` import gone from DisplaySettings/Sync too. Settings CSS now down to 5 classes (heading + supply-level chips + scan list — genuinely un-libraryable). `.actions`/`.scannerActions` gap snapped 12px→16px (md, Carl's call). `pnpm check` green. Moves Settings toward principle #10. Visual check outstanding |
| D   | **`Stack` bypassed** — ~16 `flex-direction:column` divs                       | ⬜       | Folds into #1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| E   | **`DateField` bypassed** — `TextField type="date"` in 4 files                 | ⬜       | Folds into #6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

---

## Tier 4 — Decisions to surface (KDD-governed; flag, don't silently refactor)

| #   | Item                                                           | Progress | Notes                                                                                                                         |
| --- | -------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------- |
| I   | **Shared line-edit modal shell** (5 verticals)                 | ❓       | Full-shell extraction = team call vs KDD. Defensible bits: `createLineDraft` draft-store CRUD, responsive `FieldCluster` row  |
| II  | **`BulkDeleteButton(mutation, label)`**                        | ❓       | Was `SelectionActionModal`, deliberately inlined (`kdd/action-modal`). Narrow wrapper could reclaim ~4–5 atomic-batch deletes |
| III | **`ExportPrintButton`** (4×) and **`HoldToggle`** (4×)         | ❓       | Trivial wrappers; borderline vs explicit-composition                                                                          |
| IV  | Library **`ErrorBoundary`/`GenericErrorFallback`/`DataError`** | ❓       | Reference-parity, low urgency; app has app-level `UnexpectedErrorModal` only                                                  |

---

## Ranked shortlist (issue's recommended order of attack)

| Rank | Item                                                                                                      | Maps to                                       | Progress      |
| ---- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------- |
| 1    | `HStack` / `Stack direction`                                                                              | Tier 1 #1 (+ Tier 3 D)                        | ⬜            |
| 2    | `createListController`                                                                                    | Tier 2 #8                                     | ⏭️ #653       |
| 3    | Consolidate activity-log (adopt `ActivityLogPanel` + extract `activityLogFormat`)                         | Tier 2 #9 ⏭️ #665                             | ⏭️ #665       |
| 4    | Adopt existing `createNextItemWalk` (stocktake + inbound)                                                 | Tier 2 #10 ⏭️ #666                            | ⏭️ #666       |
| 5    | Extend `ItemSearch` for outbound picker; delete copied CSS                                                | Tier 3 A                                      | ✅ `7d231690` |
| 6    | `RecordLink` (🔵), ~~`FilterDatetimeRange`~~ (→ #663), ~~route info-popovers through `InfoTooltip`~~ (✅) | Tier 1 #3 🔵, Tier 2 #11 ⏭️ #663, Tier 3 B ✅ | 🔵 partial    |
| 7    | Surface the two KDD decisions (line-edit-modal shell, `BulkDeleteButton`)                                 | Tier 4 I, II                                  | ⬜            |
