{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window (desktop)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
