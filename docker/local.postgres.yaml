server:
  danger_allow_http: true # allow http in production mode
  old_ui_frontend_dir: "frontend/old-ui" # serve this repo's client build at /old-ui/
database:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "password"
  database_name: "omsupply-database"
logging:
  mode: All
