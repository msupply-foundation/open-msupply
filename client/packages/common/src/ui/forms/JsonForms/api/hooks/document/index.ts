import { useLocationDelete } from './useLocationDelete';
import { useLocationInsert } from './useLocationInsert';
import { useLocationUpdate } from './useLocationUpdate';
import { useLocations } from './useLocations';
import { useLocationsAll } from './useLocationsAll';
import { useNextLocation } from './useNextLocation';

export const Document = {
  useLocationDelete,
  useLocationInsert,
  useLocationUpdate,
  useLocations,
  useLocationsAll,
  useNextLocation,
};
