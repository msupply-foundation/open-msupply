import { Document } from './document';
import { Utils } from './utils';

export const useTemperatureChart = {
  document: {
    chart: Document.useTemperatureChart,
  },
  utils: {
    api: Utils.useTemperatureChartApi,
  },
};
