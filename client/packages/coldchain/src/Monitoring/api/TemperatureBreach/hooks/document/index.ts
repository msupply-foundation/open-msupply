import { useTemperatureBreaches } from './useTemperatureBreaches';
import { useUpdateTemperatureBreach } from './useUpdateTemperatureBreach';

export const Document = {
  useTemperatureBreaches,
  useUpdateTemperatureBreach,
};
