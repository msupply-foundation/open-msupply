import React, { FC, StrictMode } from 'react';
import Host from './Host';
import '@fontsource-variable/inter';

const App: FC = () => (
  <StrictMode>
    <Host />
  </StrictMode>
);

export default App;
