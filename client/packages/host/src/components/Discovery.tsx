import React from 'react';
import Viewport from './Viewport';
import { ServerDiscovery } from '@openmsupply-client/common';
export const Discovery = () => (
  <Viewport>
    <ServerDiscovery></ServerDiscovery>
  </Viewport>
);
