import {
  appAdmin,
  appCustomers,
  appDashboard,
} from '@openmsupply-client/common/src/intl/locales/en';

export const getMessages = (locale: string) => ({
  'app.admin': appAdmin,
  'app.customers': appCustomers,
  'app.dashboard': appDashboard,
});
