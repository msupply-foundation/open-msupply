query AllPrefs {
  availablePreferences {
    key
    globalOnly
    jsonFormsInputType
  }
}

query prefsByKey($key: String!) {
  centralServer {
    preferences {
      preferencesByKey(key: $key) {
        serialisedDefault

        global {
          id
          key
          value
        }

        perStore {
          id
          key
          value
          storeId
        }
      }
    }
  }
}
