# Test: Distribution — automated coverage

**Source of truth:** the `OMS-REG-DIST-*` regression cases in the (private)
`tmf-testing` repo. Behaviour IDs (e.g. `OMS-REG-DIST-04.7`) are the anchor;
each Playwright test in [`distribution-regression.spec.ts`](distribution-regression.spec.ts)
is tagged `// Covers: <id>`. The wiki `Test: Distribution` page is retired.

> This coverage table is **derived from the `// Covers:` tags** in the spec
> (✅ = a deterministic test exists; ❌ = not yet covered here — may be manual-only,
> data-dependent, or cross-store). It is intended to be regenerated by a script
> rather than hand-maintained — see the strategy doc's coverage-map step.

**Coverage: 33 / 84 behaviours** have a deterministic test.

**OMS-REG-DIST-01 — List View & Management**  _(automatable: e2e)_

| Behaviour | Test? |
|---|---|
| `DIST-01.1` List displays with a fixed number of rows per page | ✅ |
| `DIST-01.2` Page navigation moves between pages without duplicating or skipping rows | ✅ |
| `DIST-01.3` The rows-per-page selector changes the number of rows shown | ✅ |
| `DIST-01.4` Search by customer name filters the list to matching shipments only | ✅ |
| `DIST-01.5` Clearing the search restores the full list | ❌ |
| `DIST-01.6` Export downloads a CSV of all shipments (not just the current page), using the currently displayed columns | ✅ |
| `DIST-01.7` Shipments at New, Allocated, or Picked status can be deleted | ✅ |
| `DIST-01.8` Shipments at Shipped, Delivered, or Verified status cannot be deleted, and a clear error is shown | ✅ |
| `DIST-01.9` Bulk delete (master checkbox) deletes the selected shipments and the notification shows the correct count | ✅ |
| `DIST-01.10` Filter by Invoice number narrows the list to matching shipments | ✅ |
| `DIST-01.11` Filter by Reference narrows the list to matching shipments | ✅ |
| `DIST-01.12` Filter by Status narrows the list to the chosen status | ✅ |
| `DIST-01.13` Clicking a page number jumps directly to that page | ✅ |

**OMS-REG-DIST-02 — Creation & Configuration**  _(automatable: e2e)_

| Behaviour | Test? |
|---|---|
| `DIST-02.1` Selecting a customer auto-creates the shipment and opens it | ✅ |
| `DIST-02.2` The Customer field is editable on the created shipment | ❌ |
| `DIST-02.3` The Customer Ref field is editable and saved | ✅ |
| `DIST-02.4` A selected colour is saved and displayed | ✅ |
| `DIST-02.5` A comment is saved and displayed | ✅ |
| `DIST-02.6` A transport reference is saved and displayed | ✅ |
| `DIST-02.7` Service charge total auto-calculates as Amount + (Amount × Tax%) | ❌ |
| `DIST-02.8` Service charges can be added and deleted | ✅ |
| `DIST-02.9` The originating requisition reference appears in Related Documents when the shipment fulfils a requisition | ❌ |
| `DIST-02.10` The Hold checkbox prevents status advancement | ✅ |
| `DIST-02.11` Unchecking Hold restores the ability to advance status | ✅ |
| `DIST-02.12` The status sequence shows passed statuses in blue and the next status in grey | ❌ |
| `DIST-02.13` Hovering the status sequence reveals the status-change history | ✅ |

**OMS-REG-DIST-03 — Line Management**  _(automatable: partial)_

| Behaviour | Test? |
|---|---|
| `DIST-03.1` Items can be found by name in the Add Item field | ✅ |
| `DIST-03.2` Items can be found by code in the Add Item field | ❌ |
| `DIST-03.3` On selection, item code, name, available stock, and unit display correctly | ❌ |
| `DIST-03.4` FEFO batch selection is applied automatically (closest expiry first) | ❌ |
| `DIST-03.5` Batch-level pack quantities can be adjusted manually | ❌ |
| `DIST-03.6` The pack-size filter greys out incompatible stock lines | ❌ |
| `DIST-03.7` A warning is shown when an expired batch is included | ❌ |
| `DIST-03.8` Placeholder lines are created for quantities exceeding available stock, highlighted in blue | ❌ |
| `DIST-03.9` Confirming allocation is blocked while placeholder lines remain | ❌ |
| `DIST-03.10` "Add from master list" adds all items as zero-quantity placeholder lines | ❌ |
| `DIST-03.11` "Add from master list" shows only lists visible to the shipment customer | ❌ |
| `DIST-03.12` Issue Quantity is editable on an existing line; row totals update automatically | ❌ |
| `DIST-03.13` The item cannot be changed on an existing line (item field locked) | ✅ |
| `DIST-03.14` Lines can be deleted via checkbox + Actions menu | ✅ |
| `DIST-03.15` Column visibility settings persist per browser session | ❌ |

**OMS-REG-DIST-04 — Processing Workflow**  _(automatable: partial)_

| Behaviour | Test? |
|---|---|
| `DIST-04.1` Confirm Allocated reserves stock so it is unavailable to other shipments | ✅ |
| `DIST-04.2` Confirm Picked becomes available once the shipment is Allocated | ✅ |
| `DIST-04.3` Bulk "Allocate placeholder lines" allocates available stock using FEFO logic | ❌ |
| `DIST-04.4` Bulk allocation shows a notification of which lines were allocated and which could not be | ❌ |
| `DIST-04.5` Confirm Picked generates an Inbound Shipment in the receiving customer store | ❌ |
| `DIST-04.6` Confirm Picked locks editing and deletion of shipment lines | ❌ |
| `DIST-04.7` Confirm Shipped removes stock from inventory (SOH decreases) | ✅ |
| `DIST-04.8` Confirm Shipped makes the shipment read-only and undeletable | ✅ |
| `DIST-04.9` A delivery note / invoice is printable at Shipped status | ❌ |
| `DIST-04.10` DELIVERED is driven by the receiving store and reflects back in the originating store view | ❌ |
| `DIST-04.11` VERIFIED is driven by the receiving store and adds goods to the receiving store's inventory | ❌ |
| `DIST-04.12` Statuses can be skipped (e.g. New → Shipped directly) via the split button | ✅ |

**OMS-REG-DIST-05 — Requisitions — Auto-Generated**  _(automatable: partial)_

| Behaviour | Test? |
|---|---|
| `DIST-05.1` The requisition list view renders with its core controls (New Requisition, status header, rows-per-page) | ✅ |
| `DIST-05.2` Requisition list filters (name, status, shipment-creation status) narrow results correctly | ❌ |
| `DIST-05.3` Export downloads a CSV matching the current page display | ❌ |
| `DIST-05.4` Auto-generated requisitions show the originating Internal Order reference in Customer Reference | ❌ |
| `DIST-05.5` The Customer field is read-only on auto-generated requisitions | ❌ |
| `DIST-05.6` "Add Item" is disabled on auto-generated requisitions | ❌ |
| `DIST-05.7` SOH and AMC reflect the requesting store's values | ❌ |
| `DIST-05.8` Quantity to Supply and Comment are editable and saved | ❌ |
| `DIST-05.9` "Supply Requested" overrides all supply quantities with the requested quantities | ❌ |
| `DIST-05.10` Next/Previous item navigation is disabled at the first and last items respectively | ❌ |
| `DIST-05.11` Finalisation makes all fields read-only and blocks further outbound shipment creation | ❌ |
| `DIST-05.12` A warning is shown when finalising without a linked outbound shipment | ❌ |

**OMS-REG-DIST-06 — Requisitions — Manual Creation**  _(automatable: e2e)_

| Behaviour | Test? |
|---|---|
| `DIST-06.1` A manual requisition is created with empty Customer Reference and Comment fields | ❌ |
| `DIST-06.2` "Add Item" is enabled on manual requisitions | ✅ |
| `DIST-06.3` Multiple items can be added and appear in the sidebar list and the item table | ❌ |
| `DIST-06.4` SOH and AMC are editable and reflect requesting-store values | ❌ |
| `DIST-06.5` Requested Quantity and Quantity to Supply are editable and saved | ❌ |
| `DIST-06.6` An item Comment is editable and saved | ❌ |
| `DIST-06.7` Customer Reference is user-entered (not auto-populated) | ❌ |
| `DIST-06.8` An outbound shipment can be created from the manual requisition and appears in Related Documents | ❌ |

**OMS-REG-DIST-07 — Customer Returns**  _(automatable: partial)_

| Behaviour | Test? |
|---|---|
| `DIST-07.1` The Customer Returns list view renders with its core controls (New return, status header, rows-per-page) | ✅ |
| `DIST-07.2` A return can be initiated from an existing outbound shipment with quantity and batch pre-filled | ❌ |
| `DIST-07.3` Reason and Comment on a return line are saved correctly | ❌ |
| `DIST-07.4` A warning banner confirms the return was created manually | ✅ |
| `DIST-07.5` "Add Item" allows additional lines to be added to the return | ❌ |
| `DIST-07.6` The More menu shows the creator and the original shipment reference | ❌ |
| `DIST-07.7` The Hold checkbox prevents status advancement until unchecked | ❌ |
| `DIST-07.8` Return lines can be deleted via checkbox + Actions menu | ❌ |
| `DIST-07.9` Confirm Delivered increases Catalogue quantity, adds a View Stock line, and records a positive "Customer Return" ledger entry | ❌ |
| `DIST-07.10` No further stock changes occur at Verified status | ❌ |
| `DIST-07.11` A standalone customer return (not linked to a shipment) creates a new stock line in View Stock at Verified | ❌ |

---

# Handover notes

## How to run

- All tests: `cd client && BASE_URL=<your-base-url> yarn e2e distribution-regression --headed --workers 1` (e.g. `BASE_URL=http://localhost:3003`)
- Filter to one: `-g "search by customer"` (case-insensitive substring match on test name)
- Faster: drop `--headed` for headless (no visible browser)
- HTML report after a run: `npx playwright show-report playwright/playwright-report`

## Helpers worth knowing

- `createNewShipment(page)` — new shipment via UI, returns URL
- `addLineToShipment(page)` — Add Item with first stocked item, qty 2, includes the OK-button retry pattern (the dialog needs 2-3 OK clicks because of a React state race)
- `pickItemWithStock(page, dialog)` — walks the item autocomplete looking for one with `Available: N > 0`. Dismisses popups after picking.
- `clickConfirmAndWait(page, regex)` — clicks a footer Confirm button, handles the optional "Are you sure?" confirmation dialog, waits for network settle. Includes `page.mouse.move(0, 0)` to dismiss the status-history hover popover that otherwise blocks clicks.
- `getColumnIndex(page, headerText)` — looks up a column's zero-based index by header text. Use this instead of hard-coded `td.nth(N)` so tests survive column reordering.
- `assertFieldPersistsAcrossReload(page, shipmentUrl, testid, value)` — fills a debounced detail-view text field, waits for the `updateOutboundShipment` mutation, reloads via `shipmentUrl`, and asserts the value persisted.
- `openSidebar(page)` — widens the viewport to ≥1536px so the responsive detail panel auto-opens. Locators inside the sidebar resolve even when it's at width:0, but clicks land underneath; call this before interacting with anything in the sidebar.

## Patterns / gotchas that took real time to discover

1. **`.MuiDialog-root` is unreliable** — matches multiple things (autocomplete popups, drawers, the actual dialog). Always use `getByRole('dialog', { name })` or filter by visible text.
2. **OK-button race in the Add Item dialog** — first click often gets swallowed mid-render. Always retry with `hover()` + `click()` in a loop until the dialog hides.
3. **MUI menu items use role `menuitem`, not `option`** — even when they're inside a listbox.
4. **Debounced text inputs** — Comment, Customer Ref, Transport Ref save via debounced GraphQL mutations. Don't reload immediately; wait for the actual mutation: `page.waitForRequest(req => req.postData()?.includes('updateOutboundShipment'))`.
5. **Hold short-circuit** — clicking a Confirm button on a held shipment shows the polite "Cannot change status because on hold" info toast and skips the confirmation dialog entirely. Source: `StatusChangeButton.tsx`'s `onStatusClick`.
6. **Status-history popover** — hovering the status sequence in the footer shows a popover that overlays the Confirm buttons. `page.mouse.move(0, 0)` before clicking Confirm dismisses it.
7. **Network-idle is too weak for debounced filters** — the request hasn't fired yet. Wait for the actual GraphQL request with text matching the search term.
8. **Column indices in the list table** (default order): 0 checkbox, 1 Name, 2 Status, 3 Number, 4 Created, 5 Reference, 6 Comment, 7 Total. Columns are reorderable in the UI, so tests use `getColumnIndex(page, 'Name')` to look up the position by header text rather than hard-coding `td.nth(N)`.
9. **Testids available** — prefer these over `.MuiDialog-root` / label-text traversal:
   - `detail-panel` — sidebar drawer
   - `confirmation-modal` + `confirmation-modal-ok` — "Are you sure?" and "Confirm status as X" dialogs
   - `customer-search-modal` — New Shipment customer picker
   - `add-item-modal` — Add Item dialog
   - `dialog-button-{variant}` — every `DialogButton` (ok, cancel, save, delete, …)
   - `comment-field`, `customer-reference-field`, `transport-reference-field` — sidebar/toolbar text inputs
   - `on-hold-button` — Hold toggle in footer (checkbox still nested: `.locator('input[type="checkbox"]')`)
   - `status-change-button-main`, `status-change-button-dropdown` — split-button halves
   - `status-crumbs` — footer status sequence (for hover popover tests)
10. **The dialog accept button is `OK`** — not Confirm/Yes/Delete. Use `name: 'OK', exact: true`.

## Test-data assumptions

- A populated dev datafile served on the configured `BASE_URL`, user `admin`
- At least one customer visible in the list
- Items with stock for `pickItemWithStock` to find quickly (it tries up to 30 items alphabetically)
- Tests use `test.skip()` rather than fail when the datafile lacks the right shape. Skips kick in for:
  - `filter by Reference` — no shipment with a non-empty reference
  - `pagination next-page` / `pagination page-number click` — fewer than 21 shipments
  - `cannot delete a Shipped shipment` / `Shipped: clicking a line` — no Shipped shipment in the visible page
  - `Edit service charges modal` — no default service item configured on the store
  - `Add Item: typing in the item field` — first item's name is too short to filter on
- **Tests leave behind data** — every run creates several shipments. No cleanup. Fine for dev, but as shipment count grows the pagination / filter tests could slow.

## Suggested next batches (priority order)

1. **Line operations on the outbound detail view** — the biggest remaining outbound gap. Add from Master List, Pack size dropdown, manual per-batch allocation, placeholder line on over-issue, and the bulk Allocate placeholder lines action. All single-store-doable; the last three depend on items with the right stock shape (multiple batches, low stock).
2. **Customer Returns: the from-shipment flow** — currently only the manual flow is covered. Adding this needs a Shipped-or-later outbound first, then the bulk-action "Return lines" path with reason + comment + Confirm Delivered / Verified.
3. **Customer Requisitions: auto-generated and finalise-flow** — receiving a customer requisition (needs an internal order from another store, so paired with the cross-store gap), item-edit page (SOH / AMC / Qty to Supply / Next / Previous), Finalise-without-shipment warning prompt, and the once-finalised lockdown.
4. **Customers section** — entirely untouched. Visibility list smoke test is the only single-store-doable bit.

## Out-of-scope / blocked

- **Date-range filters** (Created date, Shipped date) — MUI date picker is brittle in jsdom-style tests; would need a dedicated approach.
- **DELIVERED / VERIFIED status transitions** — need a customer-side mSupply store to confirm receipt.
- **Inbound shipment visible to customer on Picked** — same cross-store dependency.
- **Barcode scanner add-item** — hardware.
- **Sync working** — needs a second sync endpoint.
- **View modes** (Group by item switch, Show / Hide columns) — MRT internals; smoke-tested elsewhere, low signal here.
- **Print** PDFs (one or many shipments, pick slip, delivery note, invoice) — covered by Playwright download capture, but not yet wired up.
